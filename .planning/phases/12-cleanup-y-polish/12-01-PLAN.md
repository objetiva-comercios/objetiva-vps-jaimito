---
phase: 12-cleanup-y-polish
plan: 01
type: execute
wave: 1
depends_on:
  - 11-02
files_modified:
  - internal/cleanup/scheduler.go
  - configs/config.example.yaml
  - cmd/jaimito/serve.go
autonomous: true
requirements:
  - STOR-02

must_haves:
  truths:
    - "cleanup.Start acepta retention duration como parametro y llama PurgeOldReadings periodicamente"
    - "serve.go pasa la retention del config al cleanup.Start para metricas"
    - "config.example.yaml incluye seccion metrics comentada que documenta todos los campos"
  artifacts:
    - path: "internal/cleanup/scheduler.go"
      provides: "Scheduler con soporte de PurgeOldReadings para metrics retention"
    - path: "configs/config.example.yaml"
      provides: "Ejemplo de config completo con todos los campos v2.0 documentados"
---

<objective>
Conectar PurgeOldReadings con el scheduler de cleanup para que la retencion de datos de metricas se aplique automaticamente. Verificar que config.example.yaml documenta todos los campos v2.0.

Purpose: Completa el milestone v2.0 — la DB no crece indefinidamente y hay un ejemplo de config de referencia.
Output: scheduler.go con soporte de metrics purge, config.example.yaml actualizado
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrar PurgeOldReadings en el cleanup scheduler</name>
  <files>internal/cleanup/scheduler.go, cmd/jaimito/serve.go</files>
  <read_first>
    - internal/cleanup/scheduler.go
    - internal/db/metrics.go
    - cmd/jaimito/serve.go
    - internal/config/config.go
  </read_first>
  <action>
La funcion `db.PurgeOldReadings` existe pero no se llama desde ningun lugar. El scheduler actual solo purga mensajes (`purgeOldMessages`). Necesitamos integrar la purga de metrics readings usando el retention configurado.

**Cambios en `internal/cleanup/scheduler.go`:**

Modificar la firma de `Start` para aceptar un parametro opcional de retention para metricas:

```go
// Start lanza el cleanup scheduler. Purga mensajes viejos y, si metricsRetention > 0,
// tambien purga metric_readings mas viejos que metricsRetention.
// interval es tipicamente 24*time.Hour para produccion.
func Start(ctx context.Context, db *sql.DB, interval time.Duration, metricsRetention time.Duration) {
```

En el body, modificar `purgeOldMessages` para que tambien llame a `db.PurgeOldReadings` si `metricsRetention > 0`:

1. En la funcion Start, pasar metricsRetention al nuevo loop
2. Crear funcion `purgeMetrics(ctx, db, retention)` que llama `dbpkg.PurgeOldReadings` y loguea con slog
3. Llamar `purgeMetrics` en el mismo lugar que `purgeOldMessages` (startup + ticker)

Nota: El import del paquete `db` en cleanup va a colisionar con el parametro `db *sql.DB`. Renombrar el import del paquete `db` a `dbpkg`:
```go
import (
    ...
    dbpkg "github.com/chiguire/jaimito/internal/db"
)
```

**Cambios en `cmd/jaimito/serve.go`:**

En el paso 11 donde se llama `cleanup.Start`, pasar el retention de metricas:
```go
// 11. Start cleanup scheduler — purges old messages and metric readings in background.
var metricsRetention time.Duration
if cfg.Metrics != nil && cfg.Metrics.Retention != "" {
    if d, err := config.ParseDuration(cfg.Metrics.Retention); err == nil {
        metricsRetention = d
    }
}
cleanup.Start(ctx, database, 24*time.Hour, metricsRetention)
```
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go build ./... 2>&1 | head -10 && go test ./internal/cleanup/... 2>&1 | head -10 && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` compila sin errores
    - `cleanup.Start` acepta 4 parametros: ctx, db, interval, metricsRetention
    - Si metricsRetention > 0, `db.PurgeOldReadings` se llama en cada ciclo de cleanup
    - serve.go pasa metricsRetention correctamente desde cfg.Metrics.Retention
    - go test ./internal/cleanup/... pasa
  </acceptance_criteria>
  <done>PurgeOldReadings integrado en el scheduler, serve.go actualizado, build limpio</done>
</task>

<task type="auto">
  <name>Task 2: Verificar y completar config.example.yaml con documentacion v2.0</name>
  <files>configs/config.example.yaml</files>
  <read_first>
    - configs/config.example.yaml
    - internal/config/config.go
  </read_first>
  <action>
Verificar que `configs/config.example.yaml` documenta correctamente todos los campos de la seccion `metrics` que se introdujeron en v2.0.

El archivo ya tiene una seccion `metrics` comentada. Revisar que incluya:
- `retention` — tiempo de retencion de readings (ej "7d")
- `alert_cooldown` — tiempo minimo entre alertas para la misma metrica (ej "30m")
- `collect_interval` — intervalo de recoleccion por defecto (ej "60s")
- `definitions` — array de metricas con todos los campos documentados:
  - `name` — nombre unico
  - `command` — comando shell a ejecutar
  - `interval` — intervalo de recoleccion (opcional, hereda collect_interval)
  - `category` — categoria (system, docker, custom)
  - `type` — tipo de metrica (gauge, counter)
  - `thresholds.warning` — valor numerico para alerta warning
  - `thresholds.critical` — valor numerico para alerta critical

Si alguno de estos campos no esta documentado en el ejemplo comentado, agregarlo con un comentario explicativo. Los campos ya estan bien documentados en el archivo actual — verificar y ajustar si es necesario.

Asegurar que el ejemplo incluya al menos 2 metricas custom de ejemplo (adicionales a las predefinidas) para demostrar el patron de uso.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && grep -c "retention" configs/config.example.yaml && grep -c "alert_cooldown" configs/config.example.yaml && grep -c "collect_interval" configs/config.example.yaml && grep -c "thresholds" configs/config.example.yaml && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - configs/config.example.yaml contiene todos los campos: retention, alert_cooldown, collect_interval, definitions
    - Cada MetricDef tiene: name, command, interval (opcional), category, type, thresholds
    - Al menos 2 metricas custom de ejemplo documentadas
    - El YAML comentado es valido sintacticamente si se descomenta
  </acceptance_criteria>
  <done>config.example.yaml documenta completamente la seccion metrics v2.0</done>
</task>

</tasks>

<verification>
1. `go build ./...` — compila sin errores
2. `go test ./internal/cleanup/... ./internal/db/... ./internal/config/...` — tests pasan
3. `grep -c "PurgeOldReadings" internal/cleanup/scheduler.go` — retorna >= 1
4. `grep -c "retention" configs/config.example.yaml` — retorna >= 1
</verification>

<success_criteria>
- La DB no crece indefinidamente: PurgeOldReadings se llama automaticamente cada 24h
- config.example.yaml documenta todos los campos v2.0 con comentarios claros
- El binario arranca, colecta metricas, sirve dashboard y API en un VPS limpio
</success_criteria>

<output>
After completion, create `.planning/phases/12-cleanup-y-polish/12-01-SUMMARY.md`
</output>
