---
phase: 11-dashboard-web-embedido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/web/embed.go
  - internal/web/dashboard_test.go
  - internal/api/server.go
  - internal/web/vendor/alpine.min.js
  - internal/web/vendor/uplot.min.js
  - internal/web/vendor/uplot.min.css
  - scripts/build-dashboard.sh
  - internal/web/index.html
autonomous: true
requirements:
  - DASH-01
  - DASH-05

must_haves:
  truths:
    - "GET /dashboard retorna HTML con Content-Type text/html"
    - "El binario compila con go:embed sin errores incluso con index.html placeholder"
    - "El hostname del VPS se inyecta en el HTML servido (no window.location.hostname)"
    - "Alpine.js 3.15.9 y uPlot 1.6.32 estan disponibles localmente para ser inlineados en el HTML final"
  artifacts:
    - path: "internal/web/embed.go"
      provides: "go:embed directive + DashboardHandler con hostname injection"
      contains: "go:embed index.html"
    - path: "internal/web/dashboard_test.go"
      provides: "Test stubs para DashboardHandler y embed assets"
      contains: "TestDashboardHandler"
    - path: "internal/api/server.go"
      provides: "Ruta GET /dashboard registrada"
      contains: "web.DashboardHandler"
    - path: "scripts/build-dashboard.sh"
      provides: "Script que descarga Tailwind CLI, compila CSS, ensambla HTML final"
      contains: "tailwindcss"
    - path: "internal/web/vendor/alpine.min.js"
      provides: "Alpine.js 3.x vendoreado"
    - path: "internal/web/vendor/uplot.min.js"
      provides: "uPlot 1.6.32 IIFE vendoreado"
  key_links:
    - from: "internal/api/server.go"
      to: "internal/web/embed.go"
      via: "import + web.DashboardHandler()"
      pattern: "web\\.DashboardHandler"
    - from: "internal/web/embed.go"
      to: "internal/web/index.html"
      via: "go:embed directive"
      pattern: "go:embed index\\.html"
---

<objective>
Crear la infraestructura Go para servir el dashboard web embedido: paquete internal/web con embed.go (go:embed + DashboardHandler con hostname injection), registrar GET /dashboard en el router chi, descargar vendor libs (Alpine.js, uPlot), y crear el build script de Tailwind CSS. Incluye un index.html placeholder funcional que permite compilar el binario. Tambien crea el archivo de tests Wave 0 requerido por VALIDATION.md.

Purpose: Establece la base Go y el build pipeline para que Plan 02 pueda crear el HTML completo del dashboard sin preocuparse por la infraestructura.
Output: internal/web/embed.go, dashboard_test.go, server.go actualizado, vendors descargados, build script, index.html placeholder
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboard-web-embedido/11-CONTEXT.md
@.planning/phases/11-dashboard-web-embedido/11-RESEARCH.md
@.planning/phases/11-dashboard-web-embedido/11-VALIDATION.md

<interfaces>
<!-- API server donde se registra la ruta del dashboard -->

From internal/api/server.go:
```go
func NewRouter(database *sql.DB, cfg *config.Config) http.Handler {
    r := chi.NewRouter()
    // ...middleware...
    r.Get("/api/v1/health", HealthHandler)
    r.Get("/api/v1/metrics", MetricsListHandler(database, cfg))
    r.Get("/api/v1/metrics/{name}/readings", ReadingsHandler(database, cfg))
    // ...authenticated routes...
    return r
}
```

From internal/api/handlers.go:
```go
type MetricResponse struct {
    Name       string         `json:"name"`
    Category   string         `json:"category"`
    Type       string         `json:"type"`
    LastValue  *float64       `json:"last_value"`
    LastStatus string         `json:"last_status"`
    UpdatedAt  string         `json:"updated_at"`
    Thresholds *ThresholdInfo `json:"thresholds,omitempty"`
}

type ThresholdInfo struct {
    Warning  *float64 `json:"warning,omitempty"`
    Critical *float64 `json:"critical,omitempty"`
}

type ReadingItem struct {
    Value      float64 `json:"value"`
    RecordedAt string  `json:"recorded_at"`
}

type ReadingsListResponse struct {
    Metric   string        `json:"metric"`
    Since    string        `json:"since"`
    Readings []ReadingItem `json:"readings"`
}
```

From internal/db/db.go (patron go:embed existente):
```go
//go:embed schema/*.sql
var schemaFS embed.FS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Crear test stubs Wave 0 para internal/web (per VALIDATION.md)</name>
  <files>internal/web/dashboard_test.go</files>
  <read_first>
    - .planning/phases/11-dashboard-web-embedido/11-VALIDATION.md
    - internal/api/handlers_test.go
  </read_first>
  <action>
Crear `internal/web/dashboard_test.go` con los stubs de test documentados en VALIDATION.md. Estos tests se ejecutaran contra el codigo que las Task 1 y Task 2 crean.

```go
package web

import (
    "net/http"
    "net/http/httptest"
    "strings"
    "testing"
)

// TestDashboardHandler verifica que DashboardHandler sirve HTML con hostname inyectado (DASH-01).
func TestDashboardHandler(t *testing.T) {
    handler := DashboardHandler()
    req := httptest.NewRequest(http.MethodGet, "/dashboard", nil)
    w := httptest.NewRecorder()
    handler.ServeHTTP(w, req)

    resp := w.Result()
    if resp.StatusCode != http.StatusOK {
        t.Errorf("expected 200, got %d", resp.StatusCode)
    }
    ct := resp.Header.Get("Content-Type")
    if !strings.HasPrefix(ct, "text/html") {
        t.Errorf("expected Content-Type text/html, got %s", ct)
    }
    body := w.Body.String()
    // El placeholder {{HOSTNAME}} debe haber sido reemplazado por el hostname real
    if strings.Contains(body, "{{HOSTNAME}}") {
        t.Error("{{HOSTNAME}} placeholder was not replaced")
    }
    // El HTML debe contener contenido basico
    if !strings.Contains(body, "<!DOCTYPE html>") {
        t.Error("response does not contain <!DOCTYPE html>")
    }
}

// TestEmbedAssets verifica que el FS embedido contiene index.html (DASH-05).
func TestEmbedAssets(t *testing.T) {
    data, err := webFS.ReadFile("index.html")
    if err != nil {
        t.Fatalf("webFS.ReadFile(index.html) failed: %v", err)
    }
    if len(data) == 0 {
        t.Error("index.html is empty")
    }
}

// TestHostnameInjection verifica que el hostname se inyecta correctamente.
func TestHostnameInjection(t *testing.T) {
    handler := DashboardHandler()
    req := httptest.NewRequest(http.MethodGet, "/dashboard", nil)
    w := httptest.NewRecorder()
    handler.ServeHTTP(w, req)

    body := w.Body.String()
    // Debe contener el hostname real del sistema
    h := hostname()
    if h != "unknown" && !strings.Contains(body, h) {
        t.Errorf("expected hostname %q in response body", h)
    }
}
```

Estos tests fallan hasta que Task 1 cree embed.go con DashboardHandler y el index.html placeholder. Eso es correcto — Wave 0 crea los tests, las tareas siguientes hacen que pasen.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && test -f internal/web/dashboard_test.go && grep -c "TestDashboardHandler\|TestEmbedAssets\|TestHostnameInjection" internal/web/dashboard_test.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/web/dashboard_test.go existe
    - Contiene TestDashboardHandler (verifica DASH-01: status 200, Content-Type text/html, hostname inyectado)
    - Contiene TestEmbedAssets (verifica DASH-05: webFS contiene index.html)
    - Contiene TestHostnameInjection (verifica hostname real en el body)
  </acceptance_criteria>
  <done>Test stubs Wave 0 creados, listos para validar el codigo de Task 1</done>
</task>

<task type="auto">
  <name>Task 1: Crear paquete internal/web con embed.go, DashboardHandler y placeholder HTML</name>
  <files>internal/web/embed.go, internal/web/index.html</files>
  <read_first>
    - internal/api/server.go
    - internal/api/handlers.go
    - internal/db/db.go
    - .planning/phases/11-dashboard-web-embedido/11-RESEARCH.md
  </read_first>
  <action>
Crear directorio `internal/web/`.

Crear `internal/web/embed.go`:
```go
package web

import (
    "bytes"
    "embed"
    "net/http"
    "os"
    "sync"
)

//go:embed index.html
var webFS embed.FS

// hostname caches os.Hostname() at first call (per Pitfall 1 in RESEARCH.md).
var hostname = sync.OnceValue(func() string {
    h, err := os.Hostname()
    if err != nil {
        return "unknown"
    }
    return h
})

// DashboardHandler retorna un http.HandlerFunc que sirve index.html embedido.
// Reemplaza el placeholder {{HOSTNAME}} con el hostname real del VPS.
// Sin auth, siguiendo el patron de HealthHandler (per D-14).
func DashboardHandler() http.HandlerFunc {
    // Read the embedded HTML once at handler creation time.
    rawHTML, _ := webFS.ReadFile("index.html")

    return func(w http.ResponseWriter, r *http.Request) {
        h := hostname()
        out := bytes.ReplaceAll(rawHTML, []byte("{{HOSTNAME}}"), []byte(h))
        w.Header().Set("Content-Type", "text/html; charset=utf-8")
        w.WriteHeader(http.StatusOK)
        w.Write(out)
    }
}
```

Crear `internal/web/index.html` como placeholder funcional que permite compilar:
```html
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>jaimito — {{HOSTNAME}}</title>
</head>
<body>
    <h1>Dashboard placeholder — {{HOSTNAME}}</h1>
    <p>Plan 02 reemplaza este archivo con el dashboard completo.</p>
</body>
</html>
```

Este placeholder garantiza que `go build` funciona con la directive `//go:embed index.html`.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go test ./internal/web/ -run "TestDashboardHandler|TestEmbedAssets|TestHostnameInjection" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - internal/web/embed.go contiene `//go:embed index.html`
    - internal/web/embed.go contiene `func DashboardHandler() http.HandlerFunc`
    - internal/web/embed.go contiene `sync.OnceValue` para hostname caching
    - internal/web/embed.go contiene `bytes.ReplaceAll(rawHTML, []byte("{{HOSTNAME}}")` para inyeccion de hostname
    - internal/web/index.html contiene `{{HOSTNAME}}` como placeholder
    - `go test ./internal/web/` pasa TestDashboardHandler, TestEmbedAssets, TestHostnameInjection
  </acceptance_criteria>
  <done>El paquete internal/web compila, DashboardHandler sirve HTML con hostname inyectado, los 3 tests Wave 0 pasan</done>
</task>

<task type="auto">
  <name>Task 2: Registrar GET /dashboard en router, descargar vendors, crear build script</name>
  <files>internal/api/server.go, internal/web/vendor/alpine.min.js, internal/web/vendor/uplot.min.js, internal/web/vendor/uplot.min.css, scripts/build-dashboard.sh</files>
  <read_first>
    - internal/api/server.go
    - .planning/phases/11-dashboard-web-embedido/11-RESEARCH.md
    - .gitignore
  </read_first>
  <action>
**1. Actualizar `internal/api/server.go`:**

Agregar import del paquete web:
```go
import (
    // ...imports existentes...
    "github.com/chiguire/jaimito/internal/web"
)
```

Agregar ruta GET /dashboard en la seccion de rutas no autenticadas, DESPUES de las rutas de health y metrics:
```go
// Dashboard (unauthenticated, per D-14).
r.Get("/dashboard", web.DashboardHandler())
```

**2. Descargar vendor libraries:**

Crear directorio `internal/web/vendor/` y descargar:
- `curl -sL https://cdn.jsdelivr.net/npm/alpinejs@3.15.9/dist/cdn.min.js -o internal/web/vendor/alpine.min.js`
- `curl -sL https://cdn.jsdelivr.net/npm/uplot@1.6.32/dist/uPlot.iife.min.js -o internal/web/vendor/uplot.min.js`
- `curl -sL https://cdn.jsdelivr.net/npm/uplot@1.6.32/dist/uPlot.min.css -o internal/web/vendor/uplot.min.css`

Estos archivos se usan como referencia para pegar inline en el HTML final (Plan 02). NO se embeden directamente — se copian como `<script>` y `<style>` blocks dentro de index.html.

**3. Crear `scripts/build-dashboard.sh`:**

```bash
#!/usr/bin/env bash
# build-dashboard.sh — Descarga Tailwind CLI si no existe y compila CSS para el dashboard.
# Uso: ./scripts/build-dashboard.sh
set -euo pipefail

TOOLS_DIR="$(cd "$(dirname "$0")/.." && pwd)/tools"
WEB_DIR="$(cd "$(dirname "$0")/.." && pwd)/internal/web"
TAILWIND="$TOOLS_DIR/tailwindcss"

# Detectar arquitectura
ARCH=$(uname -m)
case "$ARCH" in
    x86_64)  TAILWIND_BIN="tailwindcss-linux-x64" ;;
    aarch64) TAILWIND_BIN="tailwindcss-linux-arm64" ;;
    *)       echo "Arquitectura no soportada: $ARCH"; exit 1 ;;
esac

# Descargar Tailwind CLI si no existe
if [ ! -x "$TAILWIND" ]; then
    echo "Descargando Tailwind CLI standalone..."
    mkdir -p "$TOOLS_DIR"
    curl -sLo "$TAILWIND" "https://github.com/tailwindlabs/tailwindcss/releases/latest/download/$TAILWIND_BIN"
    chmod +x "$TAILWIND"
    echo "Tailwind CLI descargado en $TAILWIND"
fi

# Crear input CSS para Tailwind v4
mkdir -p "$WEB_DIR/build"
echo '@import "tailwindcss";' > "$WEB_DIR/build/input.css"

# Compilar CSS escaneando index.html
"$TAILWIND" \
    --input "$WEB_DIR/build/input.css" \
    --output "$WEB_DIR/build/output.css" \
    --content "$WEB_DIR/index.html" \
    --minify

echo "CSS compilado en $WEB_DIR/build/output.css"
echo "Tamano: $(wc -c < "$WEB_DIR/build/output.css") bytes"
```

Hacer el script ejecutable: `chmod +x scripts/build-dashboard.sh`

**4. Agregar tools/ al .gitignore** si no esta ya (el binario de Tailwind tiene ~17 MB):
Agregar linea `tools/` al `.gitignore` si no existe.

**5. Verificar compilacion completa:**
Ejecutar `go build ./...` para confirmar que el import de web en server.go compila correctamente.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go build ./... 2>&1 | head -20 && test -f internal/web/vendor/alpine.min.js && test -f internal/web/vendor/uplot.min.js && test -f internal/web/vendor/uplot.min.css && test -x scripts/build-dashboard.sh && echo "ALL OK"</automated>
  </verify>
  <acceptance_criteria>
    - internal/api/server.go contiene `"github.com/chiguire/jaimito/internal/web"` en imports
    - internal/api/server.go contiene `r.Get("/dashboard", web.DashboardHandler())`
    - internal/web/vendor/alpine.min.js existe y tiene tamano > 10KB
    - internal/web/vendor/uplot.min.js existe y tiene tamano > 10KB
    - internal/web/vendor/uplot.min.css existe y tiene tamano > 500 bytes
    - scripts/build-dashboard.sh existe y es ejecutable
    - scripts/build-dashboard.sh contiene `tailwindcss` y `--content`
    - `go build ./...` compila sin errores
    - .gitignore contiene `tools/`
  </acceptance_criteria>
  <done>GET /dashboard registrado en router chi, vendors descargados, build script listo, binario compila correctamente con el dashboard placeholder</done>
</task>

</tasks>

<verification>
1. `go test ./internal/web/ -v` — tests Wave 0 pasan (TestDashboardHandler, TestEmbedAssets, TestHostnameInjection)
2. `go build ./...` compila sin errores
3. `grep -q 'go:embed index.html' internal/web/embed.go` — directive presente
4. `grep -q 'web.DashboardHandler' internal/api/server.go` — ruta registrada
5. `ls internal/web/vendor/` — 3 archivos vendor presentes
6. `test -x scripts/build-dashboard.sh` — script ejecutable
</verification>

<success_criteria>
- Tests Wave 0 pasan: TestDashboardHandler, TestEmbedAssets, TestHostnameInjection
- El binario compila con go:embed de index.html placeholder
- GET /dashboard esta registrado como ruta no autenticada en chi router
- DashboardHandler inyecta hostname del VPS via sync.OnceValue + bytes.ReplaceAll
- Alpine.js 3.15.9, uPlot 1.6.32 JS+CSS descargados en internal/web/vendor/
- Build script de Tailwind CSS listo para usar en Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-web-embedido/11-01-SUMMARY.md`
</output>
