---
phase: 05-validacion-telegram
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - cmd/jaimito/setup/general_channel_step.go
  - cmd/jaimito/setup/general_channel_step_test.go
  - cmd/jaimito/setup/extra_channels_step.go
  - cmd/jaimito/setup/extra_channels_step_test.go
  - cmd/jaimito/setup/wizard.go
autonomous: true
requirements: [TELE-02, TELE-03]

must_haves:
  truths:
    - "El operador ingresa un chat ID numerico para el canal general y ve titulo + tipo del chat si el bot tiene acceso"
    - "Un chat ID inaccesible muestra error en rojo y no avanza"
    - "El operador puede agregar 0 o mas canales extra con nombre, chat ID y prioridad"
    - "Nombres de canal duplicados o invalidos son rechazados antes de validar el chat ID"
    - "En modo edit, los canales existentes se pre-llenan y se pueden modificar"
  artifacts:
    - path: "cmd/jaimito/setup/general_channel_step.go"
      provides: "GeneralChannelStep con validacion async de chat ID"
      contains: "type GeneralChannelStep struct"
    - path: "cmd/jaimito/setup/extra_channels_step.go"
      provides: "ExtraChannelsStep con loop de canales y maquina de estados"
      contains: "type ExtraChannelsStep struct"
    - path: "cmd/jaimito/setup/general_channel_step_test.go"
      provides: "Tests unitarios para GeneralChannelStep"
      contains: "TestGeneralChannelStep"
    - path: "cmd/jaimito/setup/extra_channels_step_test.go"
      provides: "Tests unitarios para ExtraChannelsStep"
      contains: "TestExtraChannelsStep"
  key_links:
    - from: "cmd/jaimito/setup/general_channel_step.go"
      to: "data.ValidatedBot"
      via: "usa bot instanciado por BotTokenStep para GetChat"
      pattern: "data\\.ValidatedBot"
    - from: "cmd/jaimito/setup/extra_channels_step.go"
      to: "data.ValidatedBot"
      via: "usa bot instanciado por BotTokenStep para GetChat de cada canal extra"
      pattern: "data\\.ValidatedBot"
    - from: "cmd/jaimito/setup/wizard.go"
      to: "cmd/jaimito/setup/general_channel_step.go"
      via: "GeneralChannelStep reemplaza PlaceholderStep index 2"
      pattern: "&GeneralChannelStep{}"
    - from: "cmd/jaimito/setup/wizard.go"
      to: "cmd/jaimito/setup/extra_channels_step.go"
      via: "ExtraChannelsStep reemplaza PlaceholderStep index 3"
      pattern: "&ExtraChannelsStep{}"
---

<objective>
Implementar GeneralChannelStep y ExtraChannelsStep que reemplazan los PlaceholderSteps "Canal General" y "Canales Extra" en el wizard. Ambos validan chat IDs en vivo contra la API de Telegram usando el bot ya validado en el step anterior.

Purpose: TELE-02 y TELE-03 — el operador configura todos los canales de notificacion con validacion instantanea de acceso.
Output: GeneralChannelStep, ExtraChannelsStep, tests unitarios, PlaceholderSteps 2-3 reemplazados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validacion-telegram/05-CONTEXT.md
@.planning/phases/05-validacion-telegram/05-RESEARCH.md
@.planning/phases/05-validacion-telegram/05-01-SUMMARY.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Tipos y contratos del plan 05-01 que este plan consume. -->

From cmd/jaimito/setup/wizard.go (post plan 05-01):
```go
type SetupData struct {
    ConfigPath     string
    ExistingCfg    *config.Config
    ConfigErr      error
    ConfigExists   bool
    Mode           string // "new", "edit", "fresh"
    BotToken       string
    BotUsername     string
    BotDisplayName string
    ValidatedBot   *bot.Bot            // instancia reutilizable para GetChat
    Channels       []config.ChannelConfig // canales acumulados
}
```

From cmd/jaimito/setup/bot_token_step.go (creado en plan 05-01):
```go
type tokenValidationResultMsg struct {
    seq      int
    botInst  *bot.Bot
    info     telegram.BotInfo
    err      error
}
// Patron async: validationSeq, spinner.TickMsg, tea.Batch(cmd, spinner.Tick)
```

From internal/config/config.go:
```go
type ChannelConfig struct {
    Name     string `yaml:"name"`
    ChatID   int64  `yaml:"chat_id"`
    Priority string `yaml:"priority"`
}
var validPriorities = map[string]bool{
    "low": true, "normal": true, "high": true,
}
```

From internal/telegram/client.go:
```go
func ValidateChats(ctx context.Context, b *bot.Bot, channels []config.ChannelConfig) error
// bot.GetChat(ctx, &bot.GetChatParams{ChatID: chatID}) retorna *models.ChatFullInfo
// ChatFullInfo tiene: Type (models.ChatType string), Title string
```

From cmd/jaimito/setup/styles.go:
```go
var TitleStyle, ErrorStyle, HintStyle, StepDone, StepActive // todos disponibles
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GeneralChannelStep con validacion async de chat ID</name>
  <files>cmd/jaimito/setup/general_channel_step.go, cmd/jaimito/setup/general_channel_step_test.go</files>
  <read_first>
    - cmd/jaimito/setup/bot_token_step.go (patron async establecido en plan 01: tokenValidationResultMsg, validateTokenCmd, sequence number, spinner, TickMsg handling)
    - cmd/jaimito/setup/wizard.go (SetupData con ValidatedBot y Channels, Step interface)
    - cmd/jaimito/setup/styles.go (estilos disponibles)
    - internal/config/config.go (ChannelConfig struct)
    - .planning/phases/05-validacion-telegram/05-RESEARCH.md (Pattern 3: GetChat, Pitfall 4: ChatID tipo int64)
  </read_first>
  <behavior>
    - Test 1 (TestGeneralChannelStep_Init): Init crea textinput con placeholder "-1001234567890"
    - Test 2 (TestGeneralChannelStep_ValidChat): Update con chatValidationResultMsg exitoso (seq match) setea done=true, data.Channels contiene ChannelConfig{Name:"general", ChatID: -1001234567890, Priority:"normal"}
    - Test 3 (TestGeneralChannelStep_InvalidChat): Update con chatValidationResultMsg con error setea validError, done queda false
    - Test 4 (TestGeneralChannelStep_StaleResponse): Update con seq mismatch no cambia nada
    - Test 5 (TestGeneralChannelStep_InvalidFormat): Enter con texto no-numerico muestra error "chat ID invalido: debe ser numerico" sin lanzar validacion
    - Test 6 (TestGeneralChannelStep_EditModeNoChange): Modo edit con chat ID existente, Enter sin cambiar mantiene el existente
    - Test 7 (TestGeneralChannelStep_ViewValidated): View cuando done=true muestra titulo del chat y tipo
  </behavior>
  <action>
    **Archivo nuevo: `cmd/jaimito/setup/general_channel_step.go`**

    1. Tipo mensaje de resultado de validacion de chat:
       ```go
       type chatValidationResultMsg struct {
           seq       int
           chatTitle string
           chatType  string // "group", "supergroup", "channel", "private"
           chatID    int64
           err       error
       }
       ```

    2. Funcion tea.Cmd para validar chat ID:
       ```go
       func validateChatCmd(b *bot.Bot, chatID int64, seq int) tea.Cmd {
           return func() tea.Msg {
               defer func() {
                   if r := recover(); r != nil { _ = r }
               }()
               ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
               defer cancel()
               chat, err := b.GetChat(ctx, &bot.GetChatParams{ChatID: chatID})
               if err != nil {
                   return chatValidationResultMsg{seq: seq, chatID: chatID, err: err}
               }
               return chatValidationResultMsg{
                   seq: seq, chatID: chatID,
                   chatTitle: chat.Title, chatType: string(chat.Type),
               }
           }
       }
       ```
       Per D-16: timeout 10 segundos. Per Pitfall W5: panic recovery. Per Pitfall 4: chatID es int64.

    3. Funcion helper para parsear chat ID:
       ```go
       func parseChatID(raw string) (int64, error) {
           raw = strings.TrimSpace(raw)
           if raw == "" {
               return 0, fmt.Errorf("ingresa un chat ID")
           }
           id, err := strconv.ParseInt(raw, 10, 64)
           if err != nil {
               return 0, fmt.Errorf("chat ID invalido: debe ser numerico (ej: -1001234567890)")
           }
           return id, nil
       }
       ```

    4. Struct GeneralChannelStep:
       ```go
       type GeneralChannelStep struct {
           input          textinput.Model
           spinner        spinner.Model
           validationSeq  int
           validating     bool
           validError     string
           chatTitle      string  // titulo del chat validado
           chatType       string  // tipo del chat validado
           validatedID    int64   // chat ID validado
           originalChatID int64   // para modo edit: detectar si cambio
           chatChanged    bool    // true si el operador modifico el input
           done           bool
       }
       ```

    5. Init(data *SetupData) tea.Cmd:
       - Crear textinput con Placeholder "-1001234567890", CharLimit 20
       - Crear spinner con spinner.Dot per D-14
       - Si modo edit y data.ExistingCfg tiene canal "general": pre-llenar con el chat ID existente (`strconv.FormatInt(chatID, 10)`), guardar en originalChatID per D-08
       - Retornar input.Focus()

    6. Update: Mismo patron async que BotTokenStep:
       - `case spinner.TickMsg`: delegar al spinner
       - `case chatValidationResultMsg`: verificar seq, manejar error/exito. En exito: agregar `config.ChannelConfig{Name: "general", ChatID: msg.chatID, Priority: "normal"}` a `data.Channels`. Per D-06: nombre fijo "general", prioridad "normal".
       - `case tea.KeyPressMsg`:
         - Si validating: ignorar
         - Si enter: parseChatID del input, si error de formato mostrar inline (per D-05). Si modo edit y !chatChanged: mantener existente sin re-validar (per D-08). Else: lanzar validateChatCmd con data.ValidatedBot.
         - Else: delegar a input.Update, setear chatChanged=true

    7. View(data *SetupData) string:
       - Validating: spinner + "Verificando acceso al chat..." per D-14
       - Done: titulo "Canal General" + `StepDone.Render("✓ " + s.chatTitle + " (" + s.chatType + ")")` per D-07
       - Default: titulo "Canal General" + instruccion + input + error si hay + hint "Envia un mensaje al grupo y visita https://api.telegram.org/bot<TOKEN>/getUpdates para ver el chat_id" per D-05

    8. Done() bool

    **Archivo nuevo: `cmd/jaimito/setup/general_channel_step_test.go`**

    Tests per TDD, mismo patron que bot_token_step_test.go: instanciar step, Init con SetupData, enviar mensajes directamente al Update. chatValidationResultMsg simulados sin HTTP real.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go test ./cmd/jaimito/setup/... -run TestGeneralChannelStep -v</automated>
  </verify>
  <acceptance_criteria>
    - cmd/jaimito/setup/general_channel_step.go contiene `type GeneralChannelStep struct`
    - cmd/jaimito/setup/general_channel_step.go contiene `type chatValidationResultMsg struct`
    - cmd/jaimito/setup/general_channel_step.go contiene `func validateChatCmd(`
    - cmd/jaimito/setup/general_channel_step.go contiene `func parseChatID(`
    - cmd/jaimito/setup/general_channel_step.go contiene `Name: "general"` (nombre fijo per D-06)
    - cmd/jaimito/setup/general_channel_step.go contiene `Priority: "normal"` (default per D-06)
    - cmd/jaimito/setup/general_channel_step.go contiene `context.WithTimeout(context.Background(), 10*time.Second)`
    - cmd/jaimito/setup/general_channel_step.go contiene `data.ValidatedBot` (usa bot del step anterior)
    - cmd/jaimito/setup/general_channel_step.go contiene `msg.seq != s.validationSeq`
    - cmd/jaimito/setup/general_channel_step_test.go contiene `TestGeneralChannelStep_ValidChat`
    - cmd/jaimito/setup/general_channel_step_test.go contiene `TestGeneralChannelStep_InvalidChat`
    - cmd/jaimito/setup/general_channel_step_test.go contiene `TestGeneralChannelStep_StaleResponse`
    - cmd/jaimito/setup/general_channel_step_test.go contiene `TestGeneralChannelStep_EditModeNoChange`
    - `go test ./cmd/jaimito/setup/... -run TestGeneralChannelStep` exits 0
  </acceptance_criteria>
  <done>GeneralChannelStep funcional con validacion async de chat ID, nombre fijo "general", spinner, sequence number, modo edit. Tests pasando.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExtraChannelsStep con loop de canales y maquina de estados + reemplazar PlaceholderSteps</name>
  <files>cmd/jaimito/setup/extra_channels_step.go, cmd/jaimito/setup/extra_channels_step_test.go, cmd/jaimito/setup/wizard.go</files>
  <read_first>
    - cmd/jaimito/setup/general_channel_step.go (chatValidationResultMsg y validateChatCmd ya definidos, parseChatID ya definido — reusar, NO redefinir)
    - cmd/jaimito/setup/bot_token_step.go (patron async de referencia)
    - cmd/jaimito/setup/wizard.go (SetupData, Step interface, NewWizardModelWithExists — PlaceholderSteps a reemplazar)
    - cmd/jaimito/setup/steps.go (DetectConfigStep.Update como referencia de selector up/down)
    - internal/config/config.go (ChannelConfig, validPriorities)
    - .planning/phases/05-validacion-telegram/05-RESEARCH.md (Pattern 6: ExtraChannelsStep maquina de estados, Pitfall 6: canal en progreso parcial)
  </read_first>
  <behavior>
    - Test 1 (TestExtraChannelsStep_NoExtra): Init + inmediatamente responder "No" a "Agregar canal extra?" setea done=true con data.Channels sin canales extra (solo el general del step anterior)
    - Test 2 (TestExtraChannelsStep_AddOneChannel): Flujo completo: ingresar nombre "deploys" -> chat ID -> prioridad "high" -> validacion exitosa -> confirmar "No" mas canales. data.Channels tiene 1 canal extra.
    - Test 3 (TestExtraChannelsStep_DuplicateName): Nombre "general" rechazado con error (duplicado con canal obligatorio)
    - Test 4 (TestExtraChannelsStep_DuplicateExtraName): Agregar "deploys", luego intentar agregar otro "deploys" rechazado
    - Test 5 (TestExtraChannelsStep_InvalidName): Nombre con espacios o mayusculas rechazado, solo acepta `^[a-z0-9-]+$`
    - Test 6 (TestExtraChannelsStep_PrioritySelector): Selector de prioridad con up/down cambia seleccion, Enter confirma
    - Test 7 (TestExtraChannelsStep_InvalidChatID): Chat ID no-numerico muestra error inline sin lanzar validacion
    - Test 8 (TestExtraChannelsStep_EditModePreload): Modo edit pre-carga canales extra existentes del config
  </behavior>
  <action>
    **Archivo nuevo: `cmd/jaimito/setup/extra_channels_step.go`**

    IMPORTANTE: `chatValidationResultMsg`, `validateChatCmd` y `parseChatID` ya estan definidos en general_channel_step.go (mismo package). NO redefinirlos. Reusar directamente.

    1. Tipo para resultado de validacion de canal extra (reusar chatValidationResultMsg del general_channel_step.go — NO crear otro tipo):
       - Agregar un campo `channelName string` al chatValidationResultMsg si es necesario para distinguir, O usar un tipo wrapper:
       ```go
       type extraChatValidationResultMsg struct {
           chatValidationResultMsg
           channelName string
       }
       ```
       Alternativamente, si el executor determina que chatValidationResultMsg ya tiene suficiente info (chatID + seq), puede reusar sin wrapper. La decision queda al executor segun como quede la implementacion del Task 1.

    2. Constantes de estado interno:
       ```go
       type extraChannelState int
       const (
           stateAskAdd       extraChannelState = iota // "Agregar canal extra? (Si/No)"
           stateInputName                              // tipear nombre del canal
           stateInputChatID                            // tipear chat ID
           stateSelectPriority                         // elegir low/normal/high
           stateValidating                             // spinner mientras valida
           stateConfirmMore                            // "Agregar otro? (Si/No)"
       )
       ```
       Per RESEARCH.md Pattern 6.

    3. Struct ExtraChannelsStep:
       ```go
       type ExtraChannelsStep struct {
           state          extraChannelState
           nameInput      textinput.Model
           chatIDInput    textinput.Model
           spinner        spinner.Model
           validationSeq  int
           validError     string
           // Canal en progreso (variables temporales per Pitfall 6)
           currentName     string
           currentChatID   int64
           currentPriority int    // indice en priorities slice
           // Canales extra acumulados (sin incluir "general")
           extraChannels  []config.ChannelConfig
           // Selector Si/No
           confirmOption  int    // 0=Si, 1=No
           // Prioridades disponibles
           priorities     []string // ["low", "normal", "high"]
           // Chat validado info
           chatTitle      string
           chatType       string
           // Modo edit
           editChannels   []config.ChannelConfig // canales pre-cargados
           done           bool
       }
       ```

    4. Init(data *SetupData) tea.Cmd:
       - Crear nameInput con Placeholder "deploys", CharLimit 30
       - Crear chatIDInput con Placeholder "-1001234567890", CharLimit 20
       - Crear spinner con spinner.Dot
       - priorities = []string{"low", "normal", "high"}
       - currentPriority = 1 (default "normal" per D-11)
       - Si modo edit y data.ExistingCfg != nil: pre-cargar canales extra (todos menos "general") en editChannels y extraChannels. Per D-13.
       - Si hay canales pre-cargados: state = stateConfirmMore (para que el operador pueda ver los existentes y decidir agregar mas)
       - Si no hay canales pre-cargados: state = stateAskAdd ("Agregar canal extra?")
       - Retornar nil (no hay focus hasta que se necesite un input)

    5. Update: maquina de estados:
       - `case spinner.TickMsg`: delegar al spinner si state == stateValidating
       - `case chatValidationResultMsg` (o extraChatValidationResultMsg): verificar seq. En exito: crear ChannelConfig{Name: currentName, ChatID: currentChatID, Priority: priorities[currentPriority]}, agregar a extraChannels, transicionar a stateConfirmMore. En error: mostrar error, transicionar a stateInputChatID.
       - `case tea.KeyPressMsg`:
         - **stateAskAdd / stateConfirmMore**: up/down toggle entre Si/No (confirmOption 0/1). Enter: Si -> transicionar a stateInputName + focus nameInput. No -> commitear extraChannels a data.Channels (append al slice que ya tiene "general") + done=true.
         - **stateInputName**: Enter -> validar nombre con regex `^[a-z0-9-]+$`, verificar no duplicado con "general" ni con extraChannels existentes per D-10. Si valido: currentName = valor, transicionar a stateInputChatID + focus chatIDInput. Si invalido: error inline.
         - **stateInputChatID**: Enter -> parseChatID(valor). Si error formato: error inline per D-05. Si valido: currentChatID = valor, transicionar a stateSelectPriority.
         - **stateSelectPriority**: up/down cambia currentPriority (cycle entre 0,1,2). Enter -> transicionar a stateValidating, lanzar validateChatCmd(data.ValidatedBot, currentChatID, seq). Per D-11: selector de 3 opciones.
         - **stateValidating**: ignorar input
         - Delegacion a los textinputs correspondientes segun estado activo

    6. View(data *SetupData) string:
       - Titulo siempre: "Canales Extra"
       - Mostrar lista de canales extra ya agregados (si hay): nombre + chatID resumido
       - Segun state:
         - stateAskAdd: "Agregar un canal extra? (opcional)" + selector Si/No. Hint con nombres sugeridos: "Ejemplos: deploys, errors, cron, monitoring" per D-10.
         - stateInputName: "Nombre del canal:" + nameInput + error si hay + hint "Solo letras minusculas, numeros y guiones"
         - stateInputChatID: "Chat ID para '{currentName}':" + chatIDInput + error si hay
         - stateSelectPriority: "Prioridad para '{currentName}':" + lista low/normal/high con indicador > per D-11
         - stateValidating: spinner + "Verificando acceso al chat..." per D-14
         - stateConfirmMore: "Canal agregado: {name} -> {chatTitle} ({chatType})" + "Agregar otro canal?" + selector Si/No per D-09

    7. Done() bool

    **Modificar `cmd/jaimito/setup/wizard.go`:**

    En NewWizardModelWithExists, reemplazar PlaceholderSteps restantes de esta fase:
    ```go
    visibleSteps := []Step{
        &WelcomeStep{},
        &BotTokenStep{},
        &GeneralChannelStep{},    // era PlaceholderStep (reemplazado en este task)
        &ExtraChannelsStep{},     // era PlaceholderStep (reemplazado en este task)
        &PlaceholderStep{name: "Servidor"},
        &PlaceholderStep{name: "Base de Datos"},
        &PlaceholderStep{name: "Resumen"},
    }
    ```

    **Archivo nuevo: `cmd/jaimito/setup/extra_channels_step_test.go`**

    Tests per TDD. Patron: instanciar step, Init, enviar mensajes al Update para simular flujo. Para simular validacion exitosa: enviar chatValidationResultMsg directamente.

    Nombres sugeridos para canales en los tests: "deploys", "errors", "cron" (per D-10).
    Validacion de nombre: `^[a-z0-9-]+$` — rechazar "Deploy" (mayuscula), "my channel" (espacio), "" (vacio).
    Per D-12: varios canales pueden compartir el mismo chat ID.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go test ./cmd/jaimito/setup/... -run "TestExtraChannelsStep|TestGeneralChannelStep" -v && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - cmd/jaimito/setup/extra_channels_step.go contiene `type ExtraChannelsStep struct`
    - cmd/jaimito/setup/extra_channels_step.go contiene `type extraChannelState int`
    - cmd/jaimito/setup/extra_channels_step.go contiene `stateAskAdd`
    - cmd/jaimito/setup/extra_channels_step.go contiene `stateInputName`
    - cmd/jaimito/setup/extra_channels_step.go contiene `stateSelectPriority`
    - cmd/jaimito/setup/extra_channels_step.go contiene `stateValidating`
    - cmd/jaimito/setup/extra_channels_step.go contiene `stateConfirmMore`
    - cmd/jaimito/setup/extra_channels_step.go contiene regex o validacion con patron `[a-z0-9-]`
    - cmd/jaimito/setup/extra_channels_step.go contiene `data.ValidatedBot` (reusar bot)
    - cmd/jaimito/setup/extra_channels_step.go contiene `data.Channels = append(` (commitear canales al data)
    - cmd/jaimito/setup/wizard.go contiene `&GeneralChannelStep{}`
    - cmd/jaimito/setup/wizard.go contiene `&ExtraChannelsStep{}`
    - cmd/jaimito/setup/wizard.go NO contiene `PlaceholderStep{name: "Canal General"}`
    - cmd/jaimito/setup/wizard.go NO contiene `PlaceholderStep{name: "Canales Extra"}`
    - cmd/jaimito/setup/extra_channels_step_test.go contiene `TestExtraChannelsStep_NoExtra`
    - cmd/jaimito/setup/extra_channels_step_test.go contiene `TestExtraChannelsStep_AddOneChannel`
    - cmd/jaimito/setup/extra_channels_step_test.go contiene `TestExtraChannelsStep_DuplicateName`
    - cmd/jaimito/setup/extra_channels_step_test.go contiene `TestExtraChannelsStep_InvalidName`
    - cmd/jaimito/setup/extra_channels_step_test.go contiene `TestExtraChannelsStep_PrioritySelector`
    - `go test ./cmd/jaimito/setup/...` exits 0
    - `go test ./...` exits 0
  </acceptance_criteria>
  <done>GeneralChannelStep y ExtraChannelsStep funcionales reemplazando los PlaceholderSteps. Canal general con nombre fijo y validacion async. Canales extra con loop, validacion de nombre, selector de prioridad, y validacion async de chat ID. Todos los tests pasando (nuevos + existentes).</done>
</task>

</tasks>

<verification>
1. `go test ./cmd/jaimito/setup/... -v` — todos los tests pasando (previos + BotTokenStep + GeneralChannelStep + ExtraChannelsStep)
2. `go test ./... -count=1` — suite completa verde
3. `go build ./cmd/jaimito/` — compila sin errores
4. Verificar que NO quedan PlaceholderSteps para "Bot Token", "Canal General", ni "Canales Extra" en wizard.go
5. Verificar que wizard.go tiene 4 PlaceholderSteps restantes: Servidor, Base de Datos, Resumen (3 para fase 6)
</verification>

<success_criteria>
- GeneralChannelStep valida chat ID en vivo con bot.GetChat() y muestra titulo + tipo (D-07)
- Chat ID inaccesible muestra error en rojo y no avanza (TELE-02)
- ExtraChannelsStep permite loop de agregar canales con confirmacion Si/No (D-09)
- Nombres validados con ^[a-z0-9-]+$, sin duplicados (D-10)
- Selector de prioridad low/normal/high con default "normal" (D-11)
- Varios canales pueden compartir mismo chat ID (D-12)
- Modo edit pre-carga canales existentes (D-08, D-13)
- 0 canales extra es valido (TELE-03)
- PlaceholderSteps "Canal General" y "Canales Extra" reemplazados
- Todos los tests pasan (nuevos + existentes)
</success_criteria>

<output>
After completion, create `.planning/phases/05-validacion-telegram/05-02-SUMMARY.md`
</output>
