---
phase: 05-validacion-telegram
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/telegram/client.go
  - internal/telegram/client_test.go
  - cmd/jaimito/setup/wizard.go
  - cmd/jaimito/setup/steps.go
  - cmd/jaimito/setup/bot_token_step.go
  - cmd/jaimito/setup/bot_token_step_test.go
autonomous: true
requirements: [TELE-01]

must_haves:
  truths:
    - "El operador ingresa un bot token y ve @BotUsername y display name si es valido"
    - "Un token invalido muestra error en rojo y no avanza al siguiente step"
    - "En modo edit, Enter sin cambiar el token mantiene el token existente sin re-validar"
    - "Durante la validacion se muestra un spinner animado"
  artifacts:
    - path: "internal/telegram/client.go"
      provides: "ValidateTokenWithInfo() y BotInfo struct"
      contains: "func ValidateTokenWithInfo"
    - path: "cmd/jaimito/setup/bot_token_step.go"
      provides: "BotTokenStep con validacion async"
      contains: "type BotTokenStep struct"
    - path: "cmd/jaimito/setup/bot_token_step_test.go"
      provides: "Tests unitarios para BotTokenStep"
      contains: "TestBotTokenStep"
  key_links:
    - from: "cmd/jaimito/setup/bot_token_step.go"
      to: "internal/telegram/client.go"
      via: "validateTokenCmd llama telegram.ValidateTokenWithInfo"
      pattern: "telegram\\.ValidateTokenWithInfo"
    - from: "cmd/jaimito/setup/wizard.go"
      to: "cmd/jaimito/setup/bot_token_step.go"
      via: "BotTokenStep reemplaza PlaceholderStep index 1"
      pattern: "&BotTokenStep{}"
---

<objective>
Implementar ValidateTokenWithInfo() en el paquete telegram y el BotTokenStep completo que reemplaza el PlaceholderStep "Bot Token" en el wizard. El operador pega su bot token, se valida en vivo contra la API de Telegram via getMe, y ve el username y display name del bot si es valido.

Purpose: TELE-01 — primera pieza de validacion en vivo del wizard. Establece el patron async (sequence number + spinner + tea.Cmd) que los steps siguientes reusan.
Output: ValidateTokenWithInfo(), BotTokenStep funcional, tests unitarios, SetupData ampliado con campos de bot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validacion-telegram/05-CONTEXT.md
@.planning/phases/05-validacion-telegram/05-RESEARCH.md
@.planning/phases/04-wizard-scaffold/04-01-SUMMARY.md
@.planning/phases/04-wizard-scaffold/04-02-SUMMARY.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Tipos y contratos existentes que el executor necesita. -->

From cmd/jaimito/setup/wizard.go:
```go
type Step interface {
    Init(data *SetupData) tea.Cmd
    Update(msg tea.Msg, data *SetupData) (Step, tea.Cmd)
    View(data *SetupData) string
    Done() bool
}

type SetupData struct {
    ConfigPath   string
    ExistingCfg  *config.Config
    ConfigErr    error
    ConfigExists bool
    Mode         string // "new", "edit", "fresh"
}
```

From cmd/jaimito/setup/styles.go:
```go
var (
    ColorCyan   = lipgloss.Color("#00BFFF")
    ColorGreen  = lipgloss.Color("#00FF87")
    ColorRed    = lipgloss.Color("#FF5F56")
    ColorYellow = lipgloss.Color("#FFBD2E")
    ColorGray   = lipgloss.Color("#666666")
)
var (
    StepActive  = lipgloss.NewStyle().Foreground(ColorCyan).Bold(true)
    StepDone    = lipgloss.NewStyle().Foreground(ColorGreen)
    StepPending = lipgloss.NewStyle().Foreground(ColorGray)
    TitleStyle  = lipgloss.NewStyle().Foreground(ColorCyan).Bold(true)
    ErrorStyle  = lipgloss.NewStyle().Foreground(ColorRed).Bold(true)
    HintStyle   = lipgloss.NewStyle().Foreground(ColorGray)
)
```

From internal/telegram/client.go:
```go
func ValidateToken(ctx context.Context, token string) (*bot.Bot, error)
func ValidateChats(ctx context.Context, b *bot.Bot, channels []config.ChannelConfig) error
```

From internal/config/config.go:
```go
type TelegramConfig struct {
    Token string `yaml:"token"`
}
type ChannelConfig struct {
    Name     string `yaml:"name"`
    ChatID   int64  `yaml:"chat_id"`
    Priority string `yaml:"priority"`
}
type Config struct {
    Telegram    TelegramConfig  `yaml:"telegram"`
    Channels    []ChannelConfig `yaml:"channels"`
    // ...
}
```

From cmd/jaimito/setup/steps.go:
```go
func obfuscateToken(token string) string  // ya implementado
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ValidateTokenWithInfo + BotInfo en internal/telegram/client.go</name>
  <files>internal/telegram/client.go, internal/telegram/client_test.go</files>
  <read_first>
    - internal/telegram/client.go (estado actual, patron de ValidateToken existente)
    - internal/config/config.go (tipos ChannelConfig referenciados)
    - .planning/phases/05-validacion-telegram/05-RESEARCH.md (seccion "Pattern 2: ValidateTokenWithInfo", seccion "Pitfall 1: bot.New() no valida contra la API")
  </read_first>
  <behavior>
    - Test 1 (TestValidateTokenWithInfo_Format): ValidateTokenWithInfo con token vacio retorna error conteniendo "formato de token invalido"
    - Test 2 (TestBotInfo_Fields): BotInfo struct tiene campos Username string y DisplayName string
  </behavior>
  <action>
    En `internal/telegram/client.go`:

    1. Agregar el struct `BotInfo` con dos campos:
       ```go
       type BotInfo struct {
           Username    string // sin '@' — viene de models.User.Username
           DisplayName string // viene de models.User.FirstName
       }
       ```

    2. Agregar la funcion `ValidateTokenWithInfo`:
       ```go
       func ValidateTokenWithInfo(ctx context.Context, token string) (*bot.Bot, BotInfo, error) {
           b, err := bot.New(token)
           if err != nil {
               return nil, BotInfo{}, fmt.Errorf("formato de token invalido: %w", err)
           }
           me, err := b.GetMe(ctx)
           if err != nil {
               return nil, BotInfo{}, fmt.Errorf("token rechazado por Telegram: %w", err)
           }
           return b, BotInfo{
               Username:    me.Username,
               DisplayName: me.FirstName,
           }, nil
       }
       ```

    Per D-02: esta funcion retorna username y display name del bot, necesarios para mostrar al operador.
    CRITICO per Pitfall W8: `bot.New()` solo parsea el formato — DEBE llamarse `b.GetMe(ctx)` para validar contra la API real.

    En `internal/telegram/client_test.go`:
    - Test de formato: pasar token vacio a ValidateTokenWithInfo, verificar error contiene "formato de token invalido"
    - Test de struct: verificar que BotInfo{Username: "test", DisplayName: "Test"} tiene los campos correctos
    - NO testear contra la API real de Telegram (requiere red) — los tests de integracion async se hacen en bot_token_step_test.go simulando mensajes resultado
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go test ./internal/telegram/... -run TestValidateTokenWithInfo -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/telegram/client.go contiene `func ValidateTokenWithInfo(ctx context.Context, token string) (*bot.Bot, BotInfo, error)`
    - internal/telegram/client.go contiene `type BotInfo struct`
    - internal/telegram/client.go contiene `b.GetMe(ctx)` (NO solo bot.New)
    - internal/telegram/client.go contiene `fmt.Errorf("formato de token invalido`
    - internal/telegram/client.go contiene `fmt.Errorf("token rechazado por Telegram`
    - internal/telegram/client_test.go contiene `TestValidateTokenWithInfo`
    - `go test ./internal/telegram/...` exits 0
  </acceptance_criteria>
  <done>ValidateTokenWithInfo() implementada con getMe real, BotInfo struct exportado, tests unitarios pasando</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BotTokenStep + ampliar SetupData + reemplazar PlaceholderStep</name>
  <files>cmd/jaimito/setup/bot_token_step.go, cmd/jaimito/setup/bot_token_step_test.go, cmd/jaimito/setup/wizard.go, cmd/jaimito/setup/steps.go</files>
  <read_first>
    - cmd/jaimito/setup/wizard.go (SetupData actual, NewWizardModelWithExists, Step interface, como se instancian PlaceholderSteps)
    - cmd/jaimito/setup/steps.go (WelcomeStep y DetectConfigStep como patron de referencia, obfuscateToken)
    - cmd/jaimito/setup/styles.go (estilos disponibles: TitleStyle, ErrorStyle, HintStyle, StepDone)
    - internal/telegram/client.go (ValidateTokenWithInfo ya implementada en Task 1)
    - .planning/phases/05-validacion-telegram/05-RESEARCH.md (Pattern 1: Async Validation con Sequence Number, Pattern 4: Spinner, Pattern 5: textinput, Pitfall 2: stale response, Pitfall 3: spinner TickMsg, Pitfall 5: token ofuscado en modo edit)
  </read_first>
  <behavior>
    - Test 1 (TestBotTokenStep_Init): Init crea textinput con placeholder "123456789:ABCdefGhIjKlMnOpQrStUvWxYz"
    - Test 2 (TestBotTokenStep_ValidResult): Update con tokenValidationResultMsg exitoso (seq match) setea done=true, data.BotUsername="testbot", data.BotDisplayName="Test Bot"
    - Test 3 (TestBotTokenStep_InvalidResult): Update con tokenValidationResultMsg con error (seq match) setea validError, done queda false
    - Test 4 (TestBotTokenStep_StaleResponse): Update con tokenValidationResultMsg con seq!=validationSeq no cambia nada
    - Test 5 (TestBotTokenStep_EditModeNoChange): En modo edit con token existente, Enter sin cambiar token setea done=true usando token original, sin lanzar validacion
    - Test 6 (TestBotTokenStep_ViewValidated): View cuando done=true muestra "@testbot" y "Test Bot"
    - Test 7 (TestBotTokenStep_ViewValidating): View cuando validating=true muestra "Validando bot token..."
    - Test 8 (TestBotTokenStep_ViewError): View cuando hay validError muestra el error en rojo
  </behavior>
  <action>
    **Archivo nuevo: `cmd/jaimito/setup/bot_token_step.go`**

    Crear el archivo con:

    1. Tipo mensaje de resultado:
       ```go
       type tokenValidationResultMsg struct {
           seq      int
           botInst  *bot.Bot
           info     telegram.BotInfo
           err      error
       }
       ```
       Import: `"github.com/go-telegram/bot"` y `"github.com/chiguire/jaimito/internal/telegram"`

    2. Funcion tea.Cmd para validar:
       ```go
       func validateTokenCmd(token string, seq int) tea.Cmd {
           return func() tea.Msg {
               defer func() {
                   if r := recover(); r != nil {
                       // Retornar error seguro per Pitfall W5
                   }
               }()
               ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
               defer cancel()
               b, info, err := telegram.ValidateTokenWithInfo(ctx, token)
               if err != nil {
                   return tokenValidationResultMsg{seq: seq, err: err}
               }
               return tokenValidationResultMsg{seq: seq, botInst: b, info: info}
           }
       }
       ```
       Per D-16: timeout de 10 segundos.
       Per Pitfall W5: panic recovery en el Cmd.

    3. Struct BotTokenStep:
       ```go
       type BotTokenStep struct {
           input         textinput.Model
           spinner       spinner.Model
           validationSeq int
           validating    bool
           validError    string
           resolvedToken string    // token real a validar (para modo edit)
           tokenChanged  bool      // true si el operador modifico el input
           done          bool
       }
       ```

    4. Init(data *SetupData) tea.Cmd:
       - Crear `textinput.New()` con Placeholder `"123456789:ABCdefGhIjKlMnOpQrStUvWxYz"`, CharLimit 256
       - Crear `spinner.New(spinner.WithSpinner(spinner.Dot))` per D-14
       - Si `data.Mode == "edit" && data.ExistingCfg != nil`: `s.input.SetValue(obfuscateToken(data.ExistingCfg.Telegram.Token))`
       - Retornar `s.input.Focus()`

    5. Update(msg tea.Msg, data *SetupData) (Step, tea.Cmd):
       - `case spinner.TickMsg`: delegar a s.spinner.Update(msg), retornar cmd. Per Pitfall W3: el spinner DEBE recibir TickMsg.
       - `case tokenValidationResultMsg`:
         - Si `msg.seq != s.validationSeq`: retornar sin cambios (stale). Per Pitfall W2.
         - Si `msg.err != nil`: `s.validating = false`, `s.validError = msg.err.Error()`, retornar `s.input.Focus()`
         - Si exito: `s.validating = false`, `s.done = true`, setear `data.BotToken = s.resolvedToken`, `data.BotUsername = msg.info.Username`, `data.BotDisplayName = msg.info.DisplayName`, `data.ValidatedBot = msg.botInst`
       - `case tea.KeyPressMsg`:
         - Si `s.validating`: ignorar input (retornar sin cmd). Per D-14: el spinner reemplaza el input.
         - Si `msg.String() == "enter"`:
           - Obtener `token := s.input.Value()`, trim spaces
           - Si token vacio: setear validError "Ingresa un bot token", retornar
           - Si modo edit y `!s.tokenChanged`: usar `data.ExistingCfg.Telegram.Token` como token real, copiar datos existentes del bot al data, setear done=true, retornar nil. Per D-04 y Pitfall 5.
           - Else: `s.validationSeq++`, `s.validating = true`, `s.validError = ""`, `s.resolvedToken = token`, retornar `tea.Batch(validateTokenCmd(token, s.validationSeq), s.spinner.Tick)`
         - Else: delegar a `s.input.Update(msg)`, setear `s.tokenChanged = true`

    6. View(data *SetupData) string:
       - Si `s.validating`: retornar `s.spinner.View() + " Validando bot token...\n"` per D-14
       - Si `s.done`: retornar titulo "Bot Token" + `StepDone.Render("✓ @" + data.BotUsername + " (" + data.BotDisplayName + ")")` per D-02
       - Default: titulo "Bot Token" + instruccion "Pega el token de tu bot de Telegram:" + `s.input.View()` + error si hay + hint "Obtene el token de @BotFather en Telegram" per D-01, D-03

    7. Done() bool: retornar `s.done`

    **Modificar `cmd/jaimito/setup/wizard.go`:**

    1. Agregar campos a SetupData:
       ```go
       BotToken       string              // token validado (plain text)
       BotUsername    string              // @username del bot (sin @)
       BotDisplayName string              // FirstName del bot
       ValidatedBot   *bot.Bot            // instancia reutilizable para GetChat en steps siguientes
       Channels       []config.ChannelConfig // canales acumulados (general + extras)
       ```
       Agregar import `"github.com/go-telegram/bot"` en wizard.go.

    2. En NewWizardModelWithExists, reemplazar `&PlaceholderStep{name: "Bot Token"}` (index 1 del slice visibleSteps) por `&BotTokenStep{}`:
       ```go
       visibleSteps := []Step{
           &WelcomeStep{},
           &BotTokenStep{},                      // era PlaceholderStep
           &PlaceholderStep{name: "Canal General"},  // se reemplaza en plan 02
           &PlaceholderStep{name: "Canales Extra"},   // se reemplaza en plan 02
           &PlaceholderStep{name: "Servidor"},
           &PlaceholderStep{name: "Base de Datos"},
           &PlaceholderStep{name: "Resumen"},
       }
       ```

    **Archivo nuevo: `cmd/jaimito/setup/bot_token_step_test.go`**

    Crear tests per TDD. Patron de test: instanciar BotTokenStep, llamar Init con SetupData preparado, enviar mensajes directamente al Update (tokenValidationResultMsg simulados, tea.KeyPressMsg), verificar estado resultante. Per RESEARCH.md "Opcion A": testear enviando directamente los mensajes resultado, sin mock HTTP.

    ```go
    // Ejemplo de test pattern:
    func TestBotTokenStep_ValidResult(t *testing.T) {
        step := &BotTokenStep{}
        data := &SetupData{Mode: "new"}
        step.Init(data)
        step.validationSeq = 1
        step.validating = true
        msg := tokenValidationResultMsg{
            seq:     1,
            botInst: nil, // no se puede instanciar bot.Bot en test
            info:    telegram.BotInfo{Username: "testbot", DisplayName: "Test Bot"},
        }
        updated, _ := step.Update(msg, data)
        s := updated.(*BotTokenStep)
        assert s.Done() == true
        assert data.BotUsername == "testbot"
        assert data.BotDisplayName == "Test Bot"
    }
    ```

    **NO modificar** steps.go excepto si es necesario exportar obfuscateToken (ya es minuscula, accesible dentro del package setup).
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go test ./cmd/jaimito/setup/... -run TestBotTokenStep -v && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - cmd/jaimito/setup/bot_token_step.go contiene `type BotTokenStep struct`
    - cmd/jaimito/setup/bot_token_step.go contiene `type tokenValidationResultMsg struct`
    - cmd/jaimito/setup/bot_token_step.go contiene `func validateTokenCmd(`
    - cmd/jaimito/setup/bot_token_step.go contiene `context.WithTimeout(context.Background(), 10*time.Second)`
    - cmd/jaimito/setup/bot_token_step.go contiene `telegram.ValidateTokenWithInfo(`
    - cmd/jaimito/setup/bot_token_step.go contiene `spinner.New(spinner.WithSpinner(spinner.Dot))`
    - cmd/jaimito/setup/bot_token_step.go contiene `case spinner.TickMsg`
    - cmd/jaimito/setup/bot_token_step.go contiene `msg.seq != s.validationSeq`
    - cmd/jaimito/setup/wizard.go contiene `BotToken       string`
    - cmd/jaimito/setup/wizard.go contiene `BotUsername    string`
    - cmd/jaimito/setup/wizard.go contiene `ValidatedBot   *bot.Bot`
    - cmd/jaimito/setup/wizard.go contiene `&BotTokenStep{}`
    - cmd/jaimito/setup/wizard.go NO contiene `PlaceholderStep{name: "Bot Token"}`
    - cmd/jaimito/setup/bot_token_step_test.go contiene `TestBotTokenStep_ValidResult`
    - cmd/jaimito/setup/bot_token_step_test.go contiene `TestBotTokenStep_StaleResponse`
    - cmd/jaimito/setup/bot_token_step_test.go contiene `TestBotTokenStep_EditModeNoChange`
    - `go test ./cmd/jaimito/setup/...` exits 0
    - `go test ./...` exits 0
  </acceptance_criteria>
  <done>BotTokenStep funcional con validacion async, spinner, sequence number, modo edit, reemplazando el PlaceholderStep. Tests unitarios pasando. SetupData ampliado con campos de bot y canales.</done>
</task>

</tasks>

<verification>
1. `go test ./internal/telegram/... -v` — tests de ValidateTokenWithInfo pasando
2. `go test ./cmd/jaimito/setup/... -v` — todos los tests de BotTokenStep + tests existentes (14 previos) pasando
3. `go test ./... -count=1` — suite completa verde
4. `go build ./cmd/jaimito/` — compila sin errores
5. Verificar que `PlaceholderStep{name: "Bot Token"}` ya no existe en wizard.go
</verification>

<success_criteria>
- ValidateTokenWithInfo() implementada en internal/telegram/client.go con getMe real
- BotTokenStep reemplaza el PlaceholderStep "Bot Token" en el wizard
- El step valida async contra la API con spinner, sequence number, y timeout 10s
- Token invalido muestra error en rojo y no avanza (D-03)
- Modo edit pre-llena token ofuscado y Enter sin cambiar mantiene el existente (D-04)
- Token valido muestra @BotUsername y display name (D-02)
- SetupData tiene campos BotToken, BotUsername, BotDisplayName, ValidatedBot, Channels
- Todos los tests pasan (nuevos + existentes)
</success_criteria>

<output>
After completion, create `.planning/phases/05-validacion-telegram/05-01-SUMMARY.md`
</output>
