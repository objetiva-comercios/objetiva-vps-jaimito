---
phase: 09-metrics-collector-y-alertas
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - internal/collector/evaluator.go
  - internal/collector/collector.go
  - internal/collector/collector_test.go
  - internal/telegram/format.go
  - cmd/jaimito/serve.go
autonomous: true
requirements: [ALRT-02, ALRT-03, ALRT-04]

must_haves:
  truths:
    - "Cuando una metrica cruza el umbral warning por primera vez, se encola una alerta via db.EnqueueMessage al canal general"
    - "Si la metrica sigue en warning en el siguiente poll, NO se encola una segunda alerta"
    - "Cuando el estado pasa de warning a critical, se encola una nueva alerta con la transicion"
    - "Cuando el estado vuelve a ok desde warning o critical, se resetea correctamente para la proxima transicion"
    - "El cooldown configurable (default 30min) impide alertas repetidas dentro de la ventana"
    - "Al reiniciar jaimito, el state machine se rehidrata desde last_status en la DB para evitar alertas duplicadas"
    - "jaimito arranca el collector automaticamente en serve.go si cfg.Metrics != nil"
  artifacts:
    - path: "internal/collector/evaluator.go"
      provides: "State machine de alertas: metricState, evaluateLevel, shouldAlert, sendAlert"
      exports: ["metricState", "evaluateLevel"]
    - path: "internal/collector/collector.go"
      provides: "Start() actualizado con rehidratacion de estado y evaluacion de alertas"
    - path: "internal/telegram/format.go"
      provides: "Priority 'critical' con emoji rojo"
      contains: '"critical": '
    - path: "cmd/jaimito/serve.go"
      provides: "Integracion del collector en startup sequence"
      contains: "collector.Start"
  key_links:
    - from: "internal/collector/evaluator.go"
      to: "internal/db/messages.go"
      via: "db.EnqueueMessage para enviar alertas"
      pattern: "db\\.EnqueueMessage"
    - from: "internal/collector/collector.go"
      to: "internal/collector/evaluator.go"
      via: "evaluateLevel + shouldAlert en collectAndEvaluate"
      pattern: "evaluateLevel|shouldAlert"
    - from: "cmd/jaimito/serve.go"
      to: "internal/collector/collector.go"
      via: "collector.Start(ctx, database, cfg)"
      pattern: "collector\\.Start"
    - from: "internal/collector/collector.go"
      to: "internal/db/metrics.go"
      via: "db.ListMetrics para rehidratacion al arrancar"
      pattern: "db\\.ListMetrics"
---

<objective>
Agregar la state machine de alertas (evaluator.go), integrar evaluacion de umbrales en el flujo de collect-then-write, agregar priority "critical" al dispatcher, e integrar collector.Start() en serve.go.

Purpose: Sin esto las metricas se recolectan pero no se genera ninguna alerta a Telegram cuando se cruzan umbrales.
Output: evaluator.go (state machine), collector.go actualizado, format.go con "critical", serve.go con integracion
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-metrics-collector-y-alertas/09-CONTEXT.md
@.planning/phases/09-metrics-collector-y-alertas/09-RESEARCH.md
@.planning/phases/09-metrics-collector-y-alertas/09-01-SUMMARY.md

<interfaces>
<!-- Del Plan 01 — tipos y funciones creados que este plan extiende -->

From internal/collector/runner.go:
```go
func runCommand(ctx context.Context, command string, timeout time.Duration) (float64, error)
func computeTimeout(interval time.Duration) time.Duration
```

From internal/collector/collector.go:
```go
func Start(ctx context.Context, database *sql.DB, cfg *config.MetricsConfig)
func runMetricLoop(ctx context.Context, database *sql.DB, cfg *config.MetricsConfig, def config.MetricDef)
func collectAndPersist(ctx context.Context, database *sql.DB, cfg *config.MetricsConfig, def config.MetricDef)
func resolveInterval(def config.MetricDef, cfg *config.MetricsConfig) time.Duration
func category(def config.MetricDef) string
func metricType(def config.MetricDef) string
```

<!-- Interfaces existentes del proyecto -->

From internal/db/messages.go:
```go
func EnqueueMessage(ctx context.Context, db *sql.DB, id string, channel string, priority string, title *string, body string, tags []string, metadata map[string]any) error
```

From internal/db/metrics.go:
```go
func ListMetrics(ctx context.Context, db *sql.DB) ([]MetricRow, error)
func UpdateMetricStatus(ctx context.Context, db *sql.DB, name string, value float64, status string) error
func InsertReading(ctx context.Context, db *sql.DB, metricName string, value float64) error
```

From internal/telegram/format.go:
```go
var priorityEmoji = map[string]string{
    "low":    "\U0001f7e2",
    "normal": "\U0001f7e1",
    "high":   "\U0001f534",
}
```

From internal/config/config.go:
```go
type Thresholds struct {
    Warning  *float64 `yaml:"warning"`
    Critical *float64 `yaml:"critical"`
}
func ParseDuration(s string) (time.Duration, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear evaluator.go — state machine, threshold evaluation, alert sending</name>
  <files>internal/collector/evaluator.go, internal/collector/collector_test.go</files>
  <read_first>
    - internal/collector/collector.go (funciones existentes de Plan 01)
    - internal/collector/collector_test.go (tests existentes de Plan 01)
    - internal/db/metrics.go (ListMetrics, MetricRow — para rehidratacion)
    - internal/db/messages.go (EnqueueMessage firma exacta)
    - internal/config/config.go (Thresholds, MetricDef, ParseDuration)
    - internal/telegram/format.go (priorityEmoji map — para saber que agregar)
  </read_first>
  <behavior>
    - TestEvaluateLevel_NoThresholds: thresholds nil -> "ok" para cualquier valor
    - TestEvaluateLevel_Warning: value=85, warning=80, critical=90 -> "warning"
    - TestEvaluateLevel_Critical: value=95, warning=80, critical=90 -> "critical"
    - TestEvaluateLevel_Ok: value=50, warning=80, critical=90 -> "ok"
    - TestEvaluateLevel_OnlyWarning: warning=80, critical nil, value=85 -> "warning"
    - TestShouldAlert_FirstTransition: state ok, newLevel warning -> true
    - TestShouldAlert_SameLevel: state warning, newLevel warning -> false
    - TestShouldAlert_Escalation: state warning, newLevel critical -> true
    - TestShouldAlert_Recovery: state critical, newLevel ok -> true (recovery alert)
    - TestShouldAlert_CooldownBlocks: state ok, newLevel warning, lastAlert 5min ago, cooldown 30min -> false (within cooldown)
    - TestShouldAlert_CooldownExpired: state ok, newLevel warning, lastAlert 31min ago, cooldown 30min -> true
    - TestHydrateStates: ListMetrics retorna MetricRow con LastStatus="warning" -> state.currentLevel=="warning"
    - TestLevelToPriority: "warning" -> "high", "critical" -> "critical", "ok" -> "normal"
  </behavior>
  <action>
    Crear **evaluator.go** en internal/collector/ con:

    1. `type metricState struct` — Per D-09, D-10, D-11:
       ```go
       type metricState struct {
           mu           sync.Mutex
           currentLevel string    // "ok" | "warning" | "critical"
           lastAlert    time.Time // timestamp del ultimo envio de alerta
       }
       ```

    2. `func (s *metricState) shouldAlert(newLevel string, cooldown time.Duration) bool` — Per D-11:
       - Lock mutex
       - Si newLevel == s.currentLevel: retorna false (mismo nivel, no alertar)
       - Si newLevel == "ok" y s.currentLevel != "ok": retorna true (recovery)
       - Si newLevel == "ok" y s.currentLevel == "ok": retorna false
       - Si time.Since(s.lastAlert) < cooldown: retorna false (dentro de cooldown)
       - Retorna true (transicion de nivel fuera de cooldown)

    3. `func (s *metricState) transition(newLevel string)` — Actualiza currentLevel y lastAlert:
       - Lock mutex
       - s.currentLevel = newLevel
       - s.lastAlert = time.Now()

    4. `func evaluateLevel(value float64, thresholds *config.Thresholds) string`:
       - Si thresholds == nil: retorna "ok"
       - Si thresholds.Critical != nil && value >= *thresholds.Critical: retorna "critical"
       - Si thresholds.Warning != nil && value >= *thresholds.Warning: retorna "warning"
       - Retorna "ok"

    5. `func hydrateStates(ctx context.Context, database *sql.DB, defs []config.MetricDef) map[string]*metricState` — Per D-09:
       - rows, err := db.ListMetrics(ctx, database)
       - Si err != nil: slog.Error + retornar map vacio con defaults "ok"
       - Para cada def en defs: buscar en rows por name. Si encontrado y LastStatus != "", usar LastStatus; sino "ok".
       - Retornar map[string]*metricState

    6. `func levelToPriority(level string) string` — Per D-07:
       - "warning" -> "high"
       - "critical" -> "critical"
       - default -> "normal"

    7. `func sendAlert(ctx context.Context, database *sql.DB, def config.MetricDef, value float64, fromLevel, toLevel string) error` — Per D-06, D-07, D-08:
       - `hostname, _ := os.Hostname()`
       - `title := fmt.Sprintf("%s: %s -> %s", def.Name, fromLevel, toLevel)` (titulo compacto)
       - Body: `fmt.Sprintf("valor: %.2f | umbral %s: %.1f | host: %s", value, toLevel, thresholdValue, hostname)` donde thresholdValue es *def.Thresholds.Critical o *def.Thresholds.Warning segun toLevel. Para recovery (toLevel=="ok"): `fmt.Sprintf("valor: %.2f | recuperado | host: %s", value, hostname)`
       - `priority := levelToPriority(toLevel)`
       - `id := uuid.New().String()` (usar github.com/google/uuid, ya en go.mod)
       - `return db.EnqueueMessage(ctx, database, id, "general", priority, &title, body, []string{"metrics", def.Name}, nil)`

    Imports: context, database/sql, fmt, log/slog, os, sync, time, github.com/chiguire/jaimito/internal/config, github.com/chiguire/jaimito/internal/db, github.com/google/uuid

    **Agregar tests a collector_test.go** (sin borrar los existentes de Plan 01):
    - Tests unitarios puros para evaluateLevel, shouldAlert, levelToPriority
    - Test de hydrateStates con DB in-memory (upsert un metric con UpdateMetricStatus, luego verificar que hydrateStates retorna el level correcto)
    - Tests de shouldAlert con cooldown: crear metricState con lastAlert = time.Now().Add(-5*time.Minute) y verificar que cooldown de 30min bloquea, cooldown de 3min permite

    Tambien **agregar `"critical": "\U0001f534"` al map priorityEmoji** en internal/telegram/format.go:
    ```go
    var priorityEmoji = map[string]string{
        "low":      "\U0001f7e2",
        "normal":   "\U0001f7e1",
        "high":     "\U0001f534",
        "critical": "\U0001f534",
    }
    ```
    Esto hace que alertas criticas tambien usen el emoji rojo, resolviendo el Open Question 1 del RESEARCH.md.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go test ./internal/collector/... -run "TestEvaluateLevel|TestShouldAlert|TestHydrateStates|TestLevelToPriority" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/collector/evaluator.go contiene `type metricState struct`
    - internal/collector/evaluator.go contiene `func (s *metricState) shouldAlert(newLevel string, cooldown time.Duration) bool`
    - internal/collector/evaluator.go contiene `func evaluateLevel(value float64, thresholds *config.Thresholds) string`
    - internal/collector/evaluator.go contiene `func hydrateStates(`
    - internal/collector/evaluator.go contiene `func sendAlert(`
    - internal/collector/evaluator.go contiene `db.EnqueueMessage(ctx, database, id, "general"`
    - internal/collector/evaluator.go contiene `uuid.New().String()`
    - internal/collector/evaluator.go contiene `os.Hostname()`
    - internal/telegram/format.go contiene `"critical":`
    - internal/collector/collector_test.go contiene `func TestEvaluateLevel`
    - internal/collector/collector_test.go contiene `func TestShouldAlert`
    - `go test ./internal/collector/... -run "TestEvaluateLevel|TestShouldAlert|TestHydrateStates|TestLevelToPriority" -v -count=1` exits 0
  </acceptance_criteria>
  <done>evaluateLevel clasifica valores contra thresholds. shouldAlert retorna true solo en transiciones de nivel fuera de cooldown. hydrateStates carga estado desde DB. sendAlert encola mensaje con formato compacto al canal general. priorityEmoji incluye "critical". Todos los tests pasan.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar evaluator en collector.go y arrancar en serve.go</name>
  <files>internal/collector/collector.go, cmd/jaimito/serve.go, internal/collector/collector_test.go</files>
  <read_first>
    - internal/collector/collector.go (estado actual de Plan 01)
    - internal/collector/evaluator.go (recien creado en Task 1 de este plan)
    - internal/collector/collector_test.go (todos los tests existentes)
    - cmd/jaimito/serve.go (startup sequence completo — ver paso 11-12)
    - internal/config/config.go (MetricsConfig, ParseDuration)
    - internal/db/metrics.go (ListMetrics, UpdateMetricStatus)
  </read_first>
  <action>
    **Modificar collector.go:**

    1. Actualizar `Start()` — agregar rehidratacion y pasar state a cada goroutine:
       ```go
       func Start(ctx context.Context, database *sql.DB, cfg *config.MetricsConfig) {
           // Rehidratar estados desde DB (D-09)
           states := hydrateStates(ctx, database, cfg.Definitions)

           // Upsert todas las metricas
           for _, def := range cfg.Definitions {
               if err := db.UpsertMetric(ctx, database, def.Name, category(def), metricType(def)); err != nil {
                   slog.Error("upsert metric failed", "metric", def.Name, "error", err)
               }
           }

           // Una goroutine por metrica (D-01)
           for _, def := range cfg.Definitions {
               def := def
               state := states[def.Name]
               if state == nil {
                   state = &metricState{currentLevel: "ok"}
               }
               go runMetricLoop(ctx, database, cfg, def, state)
           }
       }
       ```

    2. Actualizar `runMetricLoop()` — recibir *metricState y pasar a collectAndEvaluate:
       - Firma: `func runMetricLoop(ctx context.Context, database *sql.DB, cfg *config.MetricsConfig, def config.MetricDef, state *metricState)`
       - Reemplazar llamada a collectAndPersist por collectAndEvaluate

    3. Renombrar `collectAndPersist` a `collectAndEvaluate` — agregar evaluacion de alertas:
       - Firma: `func collectAndEvaluate(ctx context.Context, database *sql.DB, cfg *config.MetricsConfig, def config.MetricDef, state *metricState)`
       - Despues de runCommand y check de error (que ya existe), agregar:
         ```go
         // 3. Evaluar nivel
         newLevel := evaluateLevel(value, def.Thresholds)

         // 4. Persistir en DB (siempre, D-10)
         if err := db.UpdateMetricStatus(ctx, database, def.Name, value, newLevel); err != nil {
             slog.Error("update metric status failed", "metric", def.Name, "error", err)
         }
         if err := db.InsertReading(ctx, database, def.Name, value); err != nil {
             slog.Error("insert reading failed", "metric", def.Name, "error", err)
         }

         // 5. Evaluar si alertar
         cooldown := 30 * time.Minute // default
         if cfg.AlertCooldown != "" {
             if d, err := config.ParseDuration(cfg.AlertCooldown); err == nil {
                 cooldown = d
             }
         }
         if state.shouldAlert(newLevel, cooldown) {
             fromLevel := state.currentLevel
             state.transition(newLevel)
             if err := sendAlert(ctx, database, def, value, fromLevel, newLevel); err != nil {
                 slog.Error("send alert failed", "metric", def.Name, "error", err)
             }
         } else if state.currentLevel != newLevel {
             state.transition(newLevel)
         }
         ```
       - Eliminar el UpdateMetricStatus con "ok" hardcodeado de Plan 01

    **Modificar serve.go:**

    Agregar import de `"github.com/chiguire/jaimito/internal/collector"`.

    Insertar DESPUES del paso 11 (cleanup.Start) y ANTES del paso 12 (HTTP server), renumerando:
    ```go
    // 12. Start metrics collector — optional, only if metrics section is configured (D-02).
    if cfg.Metrics != nil {
        collector.Start(ctx, database, cfg.Metrics)
        slog.Info("metrics collector started", "metrics_count", len(cfg.Metrics.Definitions))
    }
    ```
    Renumerar los pasos siguientes: el HTTP server pasa a ser paso 13, "Log ready state" paso 14, "Wait for shutdown" paso 15.

    **Agregar test de integracion a collector_test.go:**
    - TestCollectAndEvaluate_AlertOnTransition: DB in-memory, upsert metric "test_alert", MetricDef con Thresholds{Warning: ptr(80.0)}, state con currentLevel="ok", llamar collectAndEvaluate con Command="echo 85" -> verificar que db tiene un mensaje encolado en tabla messages (SELECT COUNT(*) FROM messages WHERE channel='general' AND body LIKE '%test_alert%')
    - TestCollectAndEvaluate_NoDoubleAlert: Despues del test anterior, llamar collectAndEvaluate de nuevo con Command="echo 85" (mismo nivel) -> verificar que no se encolo un segundo mensaje

    Funcion helper `ptr(f float64) *float64 { return &f }` para los tests.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go test ./internal/collector/... -v -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - internal/collector/collector.go contiene `states := hydrateStates(`
    - internal/collector/collector.go contiene `func collectAndEvaluate(`
    - internal/collector/collector.go contiene `evaluateLevel(value, def.Thresholds)`
    - internal/collector/collector.go contiene `state.shouldAlert(newLevel, cooldown)`
    - internal/collector/collector.go contiene `sendAlert(ctx, database, def, value, fromLevel, newLevel)`
    - internal/collector/collector.go contiene `state.transition(newLevel)`
    - internal/collector/collector.go NO contiene `func collectAndPersist(` (renombrado a collectAndEvaluate)
    - cmd/jaimito/serve.go contiene `collector.Start(ctx, database, cfg.Metrics)`
    - cmd/jaimito/serve.go contiene `"github.com/chiguire/jaimito/internal/collector"`
    - cmd/jaimito/serve.go contiene `if cfg.Metrics != nil`
    - internal/collector/collector_test.go contiene `func TestCollectAndEvaluate_AlertOnTransition`
    - `go test ./internal/collector/... -v -count=1` exits 0
    - `go build ./...` exits 0
    - `go test ./... -count=1` exits 0
  </acceptance_criteria>
  <done>collector.go rehidrata estado desde DB, evalua umbrales y envia alertas solo en transiciones. serve.go arranca el collector si cfg.Metrics != nil. Tests de integracion verifican que alertas se encolan en transicion y no se duplican. Todo compila y pasa tests.</done>
</task>

</tasks>

<verification>
```bash
# Todo compila
go build ./...

# Tests del paquete collector completos
go test ./internal/collector/... -v -count=1

# Tests de todo el proyecto pasan
go test ./... -count=1

# State machine esta implementada
grep -q 'evaluateLevel' internal/collector/evaluator.go
grep -q 'shouldAlert' internal/collector/evaluator.go
grep -q 'hydrateStates' internal/collector/evaluator.go
grep -q 'sendAlert' internal/collector/evaluator.go

# Integracion en serve.go
grep -q 'collector.Start' cmd/jaimito/serve.go
grep -q 'cfg.Metrics != nil' cmd/jaimito/serve.go

# Priority critical en format.go
grep -q '"critical"' internal/telegram/format.go

# Flujo completo: evaluateLevel -> shouldAlert -> sendAlert
grep -q 'evaluateLevel' internal/collector/collector.go
grep -q 'shouldAlert' internal/collector/collector.go
grep -q 'sendAlert' internal/collector/collector.go
```
</verification>

<success_criteria>
1. evaluateLevel clasifica correctamente valores contra thresholds (ok/warning/critical)
2. shouldAlert retorna true SOLO en transiciones de nivel, respeta cooldown
3. hydrateStates carga last_status desde DB al arrancar — evita alertas duplicadas post-restart
4. sendAlert encola mensaje al canal "general" con formato compacto (nombre + transicion + valor + hostname)
5. priorityEmoji tiene "critical" con emoji rojo
6. serve.go arranca collector.Start() si cfg.Metrics != nil, despues de cleanup.Start()
7. Alerta se encola en ok->warning, NO se re-encola en warning->warning, SI se encola en warning->critical
8. Todos los tests pasan, todo compila
</success_criteria>

<output>
After completion, create `.planning/phases/09-metrics-collector-y-alertas/09-02-SUMMARY.md`
</output>
