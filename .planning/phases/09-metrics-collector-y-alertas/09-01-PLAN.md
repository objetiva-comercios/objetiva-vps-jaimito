---
phase: 09-metrics-collector-y-alertas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/collector/runner.go
  - internal/collector/collector.go
  - internal/collector/collector_test.go
autonomous: true
requirements: [MCOL-01, MCOL-02, MCOL-04, MCOL-05]

must_haves:
  truths:
    - "Un comando shell se ejecuta con exec.CommandContext y sh -c, con timeout calculado como min(80% del intervalo, 30s) y WaitDelay de 5s"
    - "Si un comando falla o excede su timeout, el error se registra con slog y la goroutine continua al siguiente tick sin afectar otras metricas"
    - "Cada MetricDef tiene su propia goroutine con time.NewTicker independiente"
    - "El valor colectado se persiste en la DB con InsertReading y UpdateMetricStatus en cada ciclo exitoso"
  artifacts:
    - path: "internal/collector/runner.go"
      provides: "Ejecucion de comandos shell con timeout"
      exports: ["runCommand", "computeTimeout"]
    - path: "internal/collector/collector.go"
      provides: "Start() publica, goroutine-per-metric loop, collect-then-write"
      exports: ["Start"]
    - path: "internal/collector/collector_test.go"
      provides: "Tests unitarios del runner y collector"
      min_lines: 80
  key_links:
    - from: "internal/collector/runner.go"
      to: "exec.CommandContext"
      via: "sh -c con timeout y WaitDelay"
      pattern: 'exec\.CommandContext.*"sh".*"-c"'
    - from: "internal/collector/collector.go"
      to: "internal/db/metrics.go"
      via: "db.InsertReading y db.UpdateMetricStatus"
      pattern: "db\\.(InsertReading|UpdateMetricStatus)"
    - from: "internal/collector/collector.go"
      to: "internal/config/config.go"
      via: "config.MetricDef, config.ParseDuration"
      pattern: "config\\.(MetricDef|ParseDuration)"
---

<objective>
Crear el paquete internal/collector/ con la logica de ejecucion de comandos shell y el loop de recoleccion de metricas con goroutine-por-metrica.

Purpose: Es el nucleo del sistema de metricas — sin este paquete no hay recoleccion automatica.
Output: internal/collector/runner.go, internal/collector/collector.go, internal/collector/collector_test.go
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-metrics-collector-y-alertas/09-CONTEXT.md
@.planning/phases/09-metrics-collector-y-alertas/09-RESEARCH.md

<interfaces>
<!-- Tipos y funciones existentes que el executor necesita usar directamente -->

From internal/config/config.go:
```go
type MetricsConfig struct {
    Retention       string      `yaml:"retention"`
    AlertCooldown   string      `yaml:"alert_cooldown"`
    CollectInterval string      `yaml:"collect_interval"`
    Definitions     []MetricDef `yaml:"definitions"`
}

type MetricDef struct {
    Name       string      `yaml:"name"`
    Command    string      `yaml:"command"`
    Interval   string      `yaml:"interval,omitempty"`
    Category   string      `yaml:"category,omitempty"`
    Type       string      `yaml:"type,omitempty"`
    Thresholds *Thresholds `yaml:"thresholds,omitempty"`
}

type Thresholds struct {
    Warning  *float64 `yaml:"warning"`
    Critical *float64 `yaml:"critical"`
}

func ParseDuration(s string) (time.Duration, error)
```

From internal/db/metrics.go:
```go
type MetricRow struct {
    Name       string
    Category   string
    Type       string
    LastValue  *float64
    LastStatus string
    UpdatedAt  string
}

func UpsertMetric(ctx context.Context, db *sql.DB, name, category, metricType string) error
func InsertReading(ctx context.Context, db *sql.DB, metricName string, value float64) error
func UpdateMetricStatus(ctx context.Context, db *sql.DB, name string, value float64, status string) error
func ListMetrics(ctx context.Context, db *sql.DB) ([]MetricRow, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear runner.go — ejecucion de comandos con timeout</name>
  <files>internal/collector/runner.go, internal/collector/collector_test.go</files>
  <read_first>
    - internal/config/config.go (tipos MetricDef, ParseDuration)
    - internal/cleanup/scheduler.go (patron de goroutine+ticker)
    - internal/dispatcher/dispatcher.go (patron de goroutine+context)
  </read_first>
  <behavior>
    - TestComputeTimeout: interval=60s -> 30s (capped), interval=30s -> 24s (80%), interval=5s -> 4s (80%), interval=1s -> 800ms (80%)
    - TestRunCommand_Success: comando "echo 42.5" retorna 42.5, nil
    - TestRunCommand_ParseError: comando "echo notanumber" retorna 0, error conteniendo "parse output"
    - TestRunCommand_Timeout: comando "sleep 10" con timeout 100ms retorna 0, error conteniendo "command failed" (o context.DeadlineExceeded)
    - TestRunCommand_Failure: comando "exit 1" retorna 0, error conteniendo "command failed"
  </behavior>
  <action>
    Crear el directorio internal/collector/ y dos archivos:

    **runner.go** — Package collector. Contiene dos funciones:

    1. `computeTimeout(interval time.Duration) time.Duration` — Per D-05:
       - Calcula `timeout = time.Duration(float64(interval) * 0.8)`
       - Si `timeout > 30*time.Second`, retorna `30*time.Second`
       - Retorna timeout

    2. `runCommand(ctx context.Context, command string, timeout time.Duration) (float64, error)` — Per D-04:
       - Crea sub-contexto con `context.WithTimeout(ctx, timeout)` + defer cancel
       - `cmd := exec.CommandContext(cmdCtx, "sh", "-c", command)` — POSIX sh, NO bash
       - `cmd.WaitDelay = 5 * time.Second` — fuerza kill despues del timeout
       - `out, err := cmd.Output()` — captura stdout
       - Si err != nil, retorna `0, fmt.Errorf("command failed: %w", err)`
       - Parsea output con `strconv.ParseFloat(strings.TrimSpace(string(out)), 64)`
       - Si parse falla, retorna `0, fmt.Errorf("parse output %q: %w", trimmed, err)`
       - Retorna valor, nil

    Imports: context, fmt, os/exec, strconv, strings, time

    **collector_test.go** — Tests para computeTimeout y runCommand con los behaviors descritos arriba.
    Para TestRunCommand_Timeout usar timeout de 100ms con comando `sleep 10`.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go test ./internal/collector/... -run "TestComputeTimeout|TestRunCommand" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/collector/runner.go contiene `func computeTimeout(interval time.Duration) time.Duration`
    - internal/collector/runner.go contiene `func runCommand(ctx context.Context, command string, timeout time.Duration) (float64, error)`
    - internal/collector/runner.go contiene `exec.CommandContext(cmdCtx, "sh", "-c", command)`
    - internal/collector/runner.go contiene `cmd.WaitDelay = 5 * time.Second`
    - internal/collector/runner.go contiene `30 * time.Second` (cap del timeout)
    - internal/collector/collector_test.go contiene `func TestComputeTimeout`
    - internal/collector/collector_test.go contiene `func TestRunCommand_Success`
    - internal/collector/collector_test.go contiene `func TestRunCommand_Timeout`
    - `go test ./internal/collector/... -run "TestComputeTimeout|TestRunCommand" -v -count=1` exits 0
  </acceptance_criteria>
  <done>computeTimeout calcula min(80%*interval, 30s). runCommand ejecuta "sh -c" con timeout+WaitDelay y retorna float64 parseado. Todos los tests pasan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear collector.go — Start(), goroutine-per-metric, collect-then-write</name>
  <files>internal/collector/collector.go, internal/collector/collector_test.go</files>
  <read_first>
    - internal/collector/runner.go (recien creado en Task 1)
    - internal/collector/collector_test.go (tests existentes de Task 1)
    - internal/db/metrics.go (UpsertMetric, InsertReading, UpdateMetricStatus, ListMetrics)
    - internal/config/config.go (MetricsConfig, MetricDef, ParseDuration)
    - internal/dispatcher/dispatcher.go (patron goroutine+ticker — lineas 27-43)
    - internal/cleanup/scheduler.go (patron startup-then-interval — lineas 14-35)
  </read_first>
  <behavior>
    - TestResolveInterval: MetricDef con Interval="" hereda global "60s" -> 60s; MetricDef con Interval="300s" usa propio -> 300s
    - TestCategory: MetricDef con Category="" -> "custom"; con Category="system" -> "system"
    - TestMetricType: MetricDef con Type="" -> "gauge"; con Type="counter" -> "counter"
    - TestCollectAndPersist (integration con SQLite in-memory): ejecutar MetricDef con Command="echo 42.5", verificar que InsertReading y UpdateMetricStatus se llamaron correctamente (query la DB post-collect, verificar que metric_readings tiene el valor 42.5)
    - TestCollectCommandFailure: MetricDef con Command="exit 1" no produce InsertReading (metric_readings queda vacio) y no paniquea
  </behavior>
  <action>
    Crear **collector.go** con las siguientes funciones:

    1. `resolveInterval(def config.MetricDef, cfg *config.MetricsConfig) time.Duration` — Si def.Interval == "", usa cfg.CollectInterval. Parsea con config.ParseDuration(). Si hay error (no deberia, ya validado), usa 60s como fallback.

    2. `category(def config.MetricDef) string` — Retorna def.Category si != "", sino "custom".

    3. `metricType(def config.MetricDef) string` — Retorna def.Type si != "", sino "gauge".

    4. `Start(ctx context.Context, database *sql.DB, cfg *config.MetricsConfig)` — Per D-01 y D-02:
       - Llamar `db.UpsertMetric(ctx, database, def.Name, category(def), metricType(def))` para cada def en cfg.Definitions. Loguear error con slog.Error si falla, no abortar.
       - Para cada def, lanzar `go runMetricLoop(ctx, database, cfg, def)`.
       - NO retorna nada — fire-and-forget como dispatcher.Start().

    5. `runMetricLoop(ctx context.Context, database *sql.DB, cfg *config.MetricsConfig, def config.MetricDef)` — Per D-01:
       - `interval := resolveInterval(def, cfg)`
       - `ticker := time.NewTicker(interval)` + defer ticker.Stop()
       - Loop select: ticker.C -> collectAndPersist(), ctx.Done() -> return
       - Ejecutar una coleccion inmediata ANTES del loop (startup-then-interval, como cleanup.Start)

    6. `collectAndPersist(ctx context.Context, database *sql.DB, cfg *config.MetricsConfig, def config.MetricDef)` — Flujo collect-then-write per D-10:
       - `timeout := computeTimeout(resolveInterval(def, cfg))`
       - `value, err := runCommand(ctx, def.Command, timeout)`
       - Si err != nil: `slog.Error("metric collection failed", "metric", def.Name, "error", err)` y return (MCOL-05).
       - `db.UpdateMetricStatus(ctx, database, def.Name, value, "ok")` — nota: el status se evalua como "ok" en este plan; la state machine real viene en Plan 02.
       - `db.InsertReading(ctx, database, def.Name, value)`
       - Loguear errores de DB con slog.Error pero no abortar.

    Imports: context, database/sql, log/slog, time, github.com/chiguire/jaimito/internal/config, github.com/chiguire/jaimito/internal/db

    **Agregar tests a collector_test.go** (sin borrar los de Task 1):
    - Para los tests de integracion con DB, usar `db.Open(":memory:")` + `db.ApplySchema()` para tener SQLite in-memory.
    - TestCollectAndPersist: crear DB in-memory, upsert metric "test_metric", llamar collectAndPersist con Command="echo 42.5", luego verificar con db.ListMetrics que last_value=42.5 y db.QueryReadings que tiene un reading con value=42.5.
    - TestCollectCommandFailure: DB in-memory, upsert "test_fail", llamar collectAndPersist con Command="exit 1", verificar que metric_readings esta vacio (QueryReadings retorna slice vacio).
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-vps-jaimito && go test ./internal/collector/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/collector/collector.go contiene `func Start(ctx context.Context, database *sql.DB, cfg *config.MetricsConfig)`
    - internal/collector/collector.go contiene `func runMetricLoop(`
    - internal/collector/collector.go contiene `func collectAndPersist(`
    - internal/collector/collector.go contiene `func resolveInterval(`
    - internal/collector/collector.go contiene `db.UpsertMetric(`
    - internal/collector/collector.go contiene `db.InsertReading(`
    - internal/collector/collector.go contiene `db.UpdateMetricStatus(`
    - internal/collector/collector.go contiene `time.NewTicker(`
    - internal/collector/collector.go contiene `slog.Error("metric collection failed"`
    - internal/collector/collector_test.go contiene `func TestCollectAndPersist`
    - internal/collector/collector_test.go contiene `func TestCollectCommandFailure`
    - `go test ./internal/collector/... -v -count=1` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Start() lanza goroutine-por-metrica con ticker independiente. collectAndPersist ejecuta comando, persiste valor con InsertReading+UpdateMetricStatus, y falla silenciosamente en errores. Tests de integracion con SQLite in-memory pasan.</done>
</task>

</tasks>

<verification>
```bash
# Todo compila
go build ./...

# Tests del paquete collector pasan
go test ./internal/collector/... -v -count=1

# Tests del resto del proyecto no se rompen
go test ./... -count=1

# runner.go tiene los patrones clave
grep -q 'exec.CommandContext' internal/collector/runner.go
grep -q 'WaitDelay' internal/collector/runner.go

# collector.go tiene los patrones clave
grep -q 'func Start' internal/collector/collector.go
grep -q 'time.NewTicker' internal/collector/collector.go
grep -q 'db.InsertReading' internal/collector/collector.go
```
</verification>

<success_criteria>
1. internal/collector/runner.go ejecuta "sh -c" con timeout min(80%*interval, 30s) y WaitDelay=5s
2. internal/collector/collector.go lanza una goroutine por MetricDef con ticker independiente
3. Cada coleccion persiste valor en DB via InsertReading + UpdateMetricStatus
4. Fallo de un comando se loguea con slog.Error sin afectar otras metricas
5. Todos los tests (unitarios + integracion con SQLite) pasan
6. go build ./... compila sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/09-metrics-collector-y-alertas/09-01-SUMMARY.md`
</output>
